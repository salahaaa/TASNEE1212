# سجل إصلاحات B87 — من B86 إلى B87 (2026-09-05)

**الموضوع:** محرك التوزيع العادل v2 (H5/M8) + توحيد العميل الفارغ (M6) + L3.
**قرارات المستخدم المثبتة:** تخطي الجمعة افتراضياً (الأسبوع: السبت–الخميس)؛ الملء التلقائي لكل الورديات النشطة (الأساسية أولاً ثم الفيض)؛ M13 = دعم كامل متعدد الأصناف (في B88).

## 1) H5 — فلتر التحويل الرسمي والدوّار بين المسموحة (`PlanningService.cs`)
- كل دفعة تُنتَج فقط بالأصناف المسموحة من خامها حسب بطاقة المنتج (`ProductIdentityGuard.CheckConversion` — نفس الحارس المعتمد، بلا منطق جديد).
- الدوّار القديم `(CustomerId + rows) % fins` أُزيل — الآن عدّاد دوّار لكل دفعة بين أصنافها المسموحة فقط.
- الصنف المستهدف المفروض من المعالج يخضع للفلتر أيضاً: مخالف التحويل ← تُتجاوَز الدفعة بملاحظة صاخبة.
- الدفعة بلا أي صنف مسموح (أو أصنافها بلا معدل في الوضع التلقائي) تُتجاوَز مع ملاحظة ⛔ تذكر السبب والحل (بطاقة المنتج / شاشة الأصناف) — لا صفوف وهمية أبداً.

## 2) M8 — حصة كل (صنف×عبوة) بمعدلها + أرصدة واعية بالأوامر (`PlanningService.cs`)
- كل اختيار (صنف×عبوة×وردية) يُحسم بمعدله وسقفه الخاصين من `CapacityPolicy.Resolve` (محفوظ في `rateCache`) — لا معدل مرجعي واحد.
- ميزانية كل (يوم×وردية) بالساعات الفعلية + سقف الكراتين الصريح؛ الحصة اليدوية سقف كجم إجمالي لليوم عبر كل الورديات.
- الرصيد = المخزون − بنود الخطط الحية − بنود الأوامر المستقلة الحية (نفس معادلة L2 المعتمدة، تجميع ذاكري آمن لكل المزودات) — الدفعة المستنفدة لا تدخل الدوّار.
- الفتات دون العبوة الكاملة يُترَك برصيده ويُبلَّغ عنه في المتبقي بصدق — لا `Max(1,...)` ولا أي تجاوز للرصيد.

## 3) تعدد الورديات + تخطي الجمعة (`PlanningService.cs` + `PlanningWindows.cs` + `PlanningView.xaml.cs`)
- المحرك يملأ كل الورديات النشطة: الأساسية المختارة أولاً ثم الفيض للبقية — كل بند موسوم بـ `ShiftId/ShiftName`.
- تخطي الجمعة افتراضياً (وسيط `excludeFriday = true` + مربع في المعالج قابل للإلغاء)؛ الفترة كلها جمعة ← رسالة صريحة.
- نافذة الاختيار: عمود «الوردية 🕐» + معدلات وتنبيه طاقة لكل (يوم×وردية البند)؛ الإنزال يحفظ وردية كل بند.
- نافذة الملخص تعرض مصدر الأرقام (`CapacityNote`) + ملاحظات التجاوُز الصاخبة قبل المتابعة.

## 4) L3 — `DaysUsed` أيام الإنتاج الفعلية فقط (`PlanningService.cs` + `IWorkflowServices.cs`)
- الأيام الصفرية والجُمَع المستثناة لا تُحتسب؛ الغياب التام للمعدلات في الوضع التلقائي ← خروج مبكر برسالة السبب (لا «0 بنداً على N يوماً»).

## 5) M6 — العميل الفارغ end-to-end + ترحيل الصفر
- `FairPlanRowDto.CustomerId` و`FairCustomerSummaryDto.CustomerId` و`LotEditorRow.CustomerId` أصبحت `int?` — «بدون عميل» يتدفق NULL من المحرك إلى الحفظ (أُزيل `?? 0`).
- `SchemaMigrator.NullifyZeroCustomers`: ترحيل آمن تكراري 0 ← NULL في بنود الخطط/الأوامر ورأس الأمر والدفعات، مع تقرير.

## 6) الاختبارات
- `FairV2Tests.cs` (جديد — 10 اختبارات): فلتر التحويل، الفيض متعدد الورديات مع الإجمالي الدقيق، الجمعة (تخطي/إنتاج)، أسبوع بلا جمعة + `DaysUsed=4` + سقف الحصة، صنف مستهدف مخالف، خام يتيم، خطة حية تستهلك الدفعة، صدق الفتات (3 كجم)، عميل فارغ end-to-end، ترحيل الصفر.
- تحديثان سلوكيان قسريان: `PlanB77Tests` (عميل فارغ-القبول `?? -1`) + اختبار اليوم الواحد (جمعة-آمن).
- كل اختبارات Fair/B77 القديمة محفوظة الدلالة (FIFO، الأولوية، عدم الإغراق، الإجمالي الدقيق 138 طناً).

## 7) ما أُجّل لـ B88 (بقرار)
M10 (شبكة التوقفات في CloseDayDialog)، M13 (دعم الإقفال متعدد الأصناف الكامل)، تجميع استهلاك الإقفال اليومي لكل دفعة، L1 (قرار صلاحية الأمر اليدوي).

## 8) التحقق على Windows (أوامر)

```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests -c Release --filter "FullyQualifiedName~Fair|FullyQualifiedName~Plan"
dotnet test tests/DatesErp.Tests -c Release
```

**تحقق يدوي سريع:** (1) توزيع عادل لفترة تشمل جمعة ← لا بنود فيها و`DaysUsed` يستثنيها؛ (2) دفعة خامها بلا صنف مسموح ← ملاحظة ⛔ صاخبة في الملخص ولا صفوف لها؛ (3) رصيد يفوق طاقة الصباحية ← بنود في المسائية/الليلية موسومة بوردياتها؛ (4) دفعة محجوزة كلياً بخطة حية ← لا تدخل الاقتراح؛ (5) إقلاع قاعدة قديمة فيها عميل-صفر ← يتحول NULL ويظهر في تقرير الترحيل.
