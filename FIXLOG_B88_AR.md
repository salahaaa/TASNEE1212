# سجل إصلاحات B88 — من B87 إلى B88 (2026-09-05)

**الموضوع:** الإقفال متعدد الأصناف الكامل (M13) + تجميع صرف الإقفال اليومي + شبكة التوقفات (M10) + صلاحية الأمر اليدوي (L1).
**قرار المستخدم المثبت:** M13 = دعم كامل متعدد الأصناف (فحص وتوزيع لكل بند)، لا فرض أحادية.

## 1) M13 — الإقفال متعدد الأصناف (`ExecutionService.cs` + `IWorkflowServices.cs`)
- وسيط اختياري جديد `itemQtys: List<CloseItemQtyDto>` (بند ← كجم + كراتين) — يُلحَق بآخر التوقيع فلا ينكسر أي مستدعٍ قديم (18 ملف اختبار).
- عند التمرير: تجاوز الإنتاج يُفحص **لكل بند** (كجم وكراتين مع تسمية الصنف)، واتساق الكرتون/الكجم يُفحص **بهوية صنف وعبوة كل بند** — بدل فحص الإجمالي على الصنف الأول (`break` المحذوفة منطقياً).
- الكتابة مباشرة لكل بند بكميته المفحوصة — لا توزيع إجمالي أعمى بالترتيب؛ الإجماليات تُشتق من مجموع البنود.
- تنبيه معامل الإنتاجية: سطر لكل صنف بنسبة إنتاجه من المستهلك (تنبيه فقط)؛ أحادي الصنف كما كان.
- رسالة النجاح تُذيَّل بتفصيل البنود عند تعددها (📦 ...) — الشكل القديم محفوظ قبله.
- المسار الإجمالي القديم (`itemQtys = null`) باقٍ للتوافق كما هو (فحص الصنف الأول + توزيع بالترتيب) — الشاشة تمرر التفصيل دائماً.
- الفحص النوعي: فحص واحد للجلسة بالإجماليات (لا بند-جودة لكل بند — خارج قرار النطاق).

## 2) تجميع صرف الإقفال اليومي لكل دفعة (`ExecutionService.cs`)
- الصرف كان ينشر **حركة لكل بند** — بندان من دفعة واحدة بنفس المرجع = انفجار `DUPLICATE` (نفس الفخ الذي عولج في B85/B86).
- الآن: تجميع المصروف لكل دفعة أولاً ثم `ConsumeLot` + حركة صرف **واحدة** لكل دفعة (عميل الحركة = أول بنودها). مرتجع M12 يستخدم نفس التجميع — بلا تغيير.

## 3) M10 — شبكة التوقفات في حوار الإقفال (`OrdersWindows.cs` + `OrdersView.xaml.cs`)
- `CloseDayDialog` يستقبل `orderId` ويحمّل بنود الأمر (صنف/عبوة/مخطط/متبقي) — صف إدخال (كجم + كراتين) لكل بند معبأ بالمتبقي، والإجمالي = المجموع.
- شبكة توقفات قابلة للتحرير (الساعات/السبب */من/إلى HH:mm) مع زرّي إضافة/حذف — تحقق: الساعات بلا سبب مرفوضة، والوقت بصيغة `HH:mm`.
- الاستدعاءان الميتان (`new List<DowntimeDto>()`) أصبحا يمرران التوقفات المدخلة + تفصيل البنود. تعذّر التحميل = المسار الإجمالي القديم (تدهور آمن).
- تقرير كفاءة الورديات موجود مسبقاً ويقرأ `ExecutionDowntimes` — اكتملت حلقته بالإدخال.

## 4) L1 — صلاحية الأمر اليدوي (`PermissionService.cs` + `ProductionOrderService.cs`)
- القرار: مورد صلاحيات جديد `manualorder` («الأوامر اليدوية الاستثنائية (بلا خطة)») في الكتالوج — يظهر في مصفوفة الأدوار ويُمنح صراحةً.
- `SaveOrder` بلا خطة يتطلب `manualorder/Create` (بعد `production/Create`)؛ أمر الخطة لا يتأثر. المدير العام تُستكمل له تلقائياً بآلية `EnsureCatalog` القائمة — لا يُقفل النظام.
- لا زر يدوي في الواجهة أصلاً (كل الإنشاء `FromPlan`) — البوابة للخدمة/API والشاشات القادمة.

## 5) الاختبارات (`CloseB88Tests.cs` — جديد، 8 اختبارات)
إقفال صنفين بتفصيل (فحص/كتابة/إجماليات/رسالة)، رفض تجاوز بند بالتسمية، رفض تناقض كرتون بند، المسار القديم يوزع بالترتيب، دفعة واحدة ببندين = حركة صرف واحدة (لا DUPLICATE)، حفظ التوقفات بالأوقات، اليدوي (قبول/سحب/منح)، أمر الخطة يتجاهل صلاحية اليدوي.

## 6) التحقق على Windows (أوامر)

```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests -c Release --filter "FullyQualifiedName~Close|FullyQualifiedName~Order|FullyQualifiedName~Permission|FullyQualifiedName~Parity"
dotnet test tests/DatesErp.Tests -c Release
```

**تحقق يدوي سريع:** (1) إقفال يوم لأمر صنفين ← صف لكل بند + تفصيل في الرسالة؛ (2) بند بكرتون مخالف لعبوته ← رفض مسمّى بالصنف؛ (3) إضافة توقف بساعات بلا سبب ← رفض، وبسبب ← يظهر في تقرير الكفاءة؛ (4) مستخدم بلا `manualorder` ← اليدوي مرفوض «لا تملك صلاحية»، وأمر الخطة يعمل؛ (5) أمر ببندين من دفعة واحدة ← إقفال ناجح بلا DUPLICATE.
