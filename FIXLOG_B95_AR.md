# سجل إعادة التطوير B95 — دورة الفحص/الجودة + توحيد إقفال خطة الإنتاج (2026-09-05)

## §0 — القرار (طلب الإدارة)
لا متابعة للتصميم السابق — إعادة تطوير مهنية لمسارين في حزمة واحدة:
(أ) دورة الفحص/الجودة (خطة ← أمر ← إنتاج ← فحص ← تام ← تسليم)،
(ب) توحيد إقفال خطة الإنتاج في مسار واحد. مع بقاء قاعدة التبريد:
استلام التام مسموح عند الإقفال، وتسليم العميل بانتظار اعتماد الفحص.

## §1 — دورة الفحص/الجودة (إعادة تطوير)
- أمر فحص معلَّق يُنشأ من الإقفال اليومي (إرسال للجودة): الحالة «مُرسل للفحص»،
  والتاريخ المتوقع = يوم الإقفال + 2 (فترة التبريد)، وكمية الفحص = المنتَج الفعلي.
- رأس المحضر: تام فقط (المجموعة 002) + تاريخ الإنتاج من الأمر (بَدَل «الصنف الخام») —
  كل الحقول تُجلب آلياً ولا تُعاد إدخالاً.
- معادلة التلخيص الحية: المفحوص = مقبول + مرفوض (1000 = 900 + 80 + 20) —
  تُشتق تلقائياً عند الإغفال وتُرفض عند الاختلال، مع نسب ٪ تلقائية من المنتَج.
- بوابات الحفظ: لا أمر مجهول، لا فحص قبل الإنتاج، لا تجاوز للمنتَج (سقف لكل صنف)،
  لا سالب، لا صنف خام، لا تغطية مزدوجة (مجموع فحوصات الأمر ≤ إنتاجه).
- الاكتمال ذاتي أو تراكمي: فحصان جزئيان يتكاملان (300 + 200 = 500) وكلاهما قابل للاعتماد —
  بلا التراكمي يجمُد الأمر (فحصان «قيد الفحص» لا يُعتمد أيهما أبداً).
- حالات المحضر: مسودة ← قيد الفحص ← مكتمل ← معتمد (`QualityCheckStatuses.ToArabic` الجديد —
  `DocStatuses.ToArabic` المثبت لم يُمس).
- التصحيح بعد الاعتماد: صلاحية «الجودة/تعديل بعد الاعتماد» (كانت ميتة — فُعّلت وتُمنح
  آلياً لمعتمدي الجودة عند الإقلاع) + سبب مكتوب إجباري + صف `QualityCorrection` + سجل تدقيق —
  ثم يُعاد المحضر «قيد الفحص» للتعديل وإعادة الاعتماد.
- سقف التسليم: لا يُسلَّم للعميل أكثر من «المطابق المعتمد» (مقبول الفحوصات المعتمدة
  ناقص ما سُلِّم سابقاً) — بعد فحص الرصيد لتبقى رسائله، وقبل الخصم.
- درجات النتيجة: مطابق / غير مطابق (قابل للمعالجة) / مرفوض نهائي (عوادم) —
  يُفرَّق بينهما بعلم `IsFinalScrap` في إعدادات أنواع النتائج.

## §2 — توحيد الإقفال (المسار الواحد)
- حُذف `ClosePlanItems` نهائياً (~320 سطراً): مسار موازٍ ميت (لا تستدعيه أي شاشة)
  يكرر منطق الإقفال اليومي برياضيات مختلفة. جداول `PlanClosing*` باقية للقراءة التاريخية فقط.
- المسار الرسمي الوحيد: `CloseProductionDay` عبر أمر الإنتاج — وقد استوعب منه:
  تسوية المواد المساعدة مقابل الفعلي (مرتجع/صرف آلي موثق + مواد الإدخال الفعلي كالديزل)،
  وتوريد الكرتون الفارغ (فعلي مؤكد أو تقدير من الخام المصروف)، وتحمل الدفعات بلا نوع عبوة.
- إغلاق الأمر: مكتمل الإنتاج يُغلق مباشرة؛ الناقص يتطلب سبب تسوية موثقاً
  يُحفظ في `CloseReason` ويظهر في فروقات الخطة (رسالة `INCOMPLETE_ORDER` توجه للإدخال).
  بنود `IsClosed` الموروثة تُحترم للبيانات القديمة فقط.
- إقفال يوم بلا إنتاج مرفوض (حماية من الإقفال الفارغ بالخطأ).
- إصلاحات دفترية ورثها المسار المحذوف: الكميات موقعة (الصرف سالب) في حركات التسوية،
  والرصيد المساعد إجمالي بلا عميل (كصرف الاعتماد) — فيبقى تدقيق الدفتر متسقاً.
- شاشة الأمر: زر الإغلاق يطلب السبب (فارغ عند الاكتمال)؛ حوار الإقفال اليومي يعرض
  المواد المصروفة مع إدخال الفعلي المستهلك + الكرتون الفارغ الفعلي (اختياريان).

## §3 — الملفات (مقابل B94)
خدمات: `ExecutionService.cs` (التسوية + التوريد + بوابات الفحص + الاكتمال التراكمي +
التصحيح المعتمد + حذف المسار)، `ProductionOrderService.cs` (الإغلاق بتسوية موثقة)،
`InspectionService.cs` (الدرجات + ملخص المعادلة)، `QualityGate.cs` (سقف المطابق المعتمد)،
`PermissionService.cs` (منح التصحيح لمعتمدي الجودة)، `CustomerDeliveryService.cs` (استدعاء السقف).
كيانات/واجهات: `Production.cs` (`CloseReason`)، `Quality.cs` (حالات المحضر + الكراتين +
`QualityCorrection` + اسم الفاحص)، `Inspection.cs` (درجات الرفض)، `IWorkflowServices.cs`
(حذف المسار + حقول الكراتين + `RequestCorrection` + ملخص الدرجات).
واجهات: `OrdersWindows.cs` (سبب الإغلاق + تسوية المواد في حوار الإقفال)، `OrdersView.xaml.cs`
(تمرير التسوية)، `QualityView.xaml(.cs)` (تاريخ الإنتاج + المعادلة الحية + التصحيح المعتمد)،
`QualitySetupWindow.cs` (علم المرفوض النهائي)، `BuildInfo.cs` (الختم B95).
قاعدة: `DatesErpDbContext.cs` (`QualityCorrections`).
اختبارات: جديد `B95RedevelopmentTests.cs` (8 اختبارات)؛ مُرحَّلة للمسار الواحد:
`AuxFlexTests`، `ClosingParityTests`، `ClosingPreservesPlanBaselineTests`، `ExecutionB57Tests`،
`MasterLinkageTests`، `PlanClosingTests`، `PlanClosureTests`، `PlanLevelClosingTests`.

## §4 — التحقق على ويندوز (تسلسلياً)
```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~B95RedevelopmentTests"
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release
```
فحص يدوي: أقفل يوم أمر وأرسله للجودة — فحص معلَّق بتاريخ +2؛ سجل نتيجة مخالفة للمعادلة —
رفض يسمي الصنف؛ اعتمد الفحص ثم صححه بسبب — صف تصحيح + تدقيق؛ أغلق أمراً ناقصاً بلا سبب —
رفض موجَّه، وبسبب — تسوية موثقة في الأمر وفروقات الخطة.
