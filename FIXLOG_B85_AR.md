# سجل إصلاحات B85 — من B84 إلى B85 (2026-09-05)

> النطاق: البنود العاجلة من تقرير مراجعة دورة التخطيط (`PRODUCTION_PLANNING_REVIEW_AR.md`):
> H1 + H2 + H3 + H4 + H6، مع البنود الصفرية-الخطر M7 وM9 وM3.
> محرك التوزيع العادل (H5/M8) وبقية البنود المتوسطة مؤجلة لـ B86 بقرار موثق آخر هذا الملف.
> التنفيذ عمياني (بلا مُصرّف/قاعدة هنا) — كل تغيير سلوكي موثق أدناه مع خطوات التحقق على Windows.

## 1) H1 — توحيد مساري الإقفال ومنع الازدواج (`ExecutionService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| حارس المسار الأول | `CloseProductionDay` يرفض الأمر الذي أُقفلت بنوده عبر «إقفال بنود الخطة» (`oi.IsClosed`) برسالة توجيهية — منع ازدواج الإنتاج والخام. |
| حارس المسار الثاني | `ClosePlanItems` يرفض البند الذي سُجّل إنتاجه عبر مسار الأوامر (`oi.ProducedQtyKg > 0`) — كود `DUAL_CLOSE`. |
| صرف الخام الموحد | `ClosePlanItems` كان يُسجَّل الإنتاج **دون صرف الخام إطلاقاً** (خلاف مسار الأوامر). الآن يصرف `consumed` (حصة الأمر أو مخطط البند) بحركة `Outbound` موثقة — **مجمّعة لكل دفعة بحركة واحدة** (بندان من دفعة واحدة لا يكسرا منع التكرار). |
| نهاية «صفر = المتبقي كله» | الأصفار الكاملة مع متبقٍّ = **تحرير البند** (إسقاط المتبقي في `ReleasedQtyKg`) لا إنتاجه — كان يُسجَّل إنتاجاً وهمياً. والمنتَج = المُدخل فقط. |
| رفض صريح جديد | كراتين مدخلة بلا كيلو (`ProducedKg = 0` + `ProducedCartons > 0`) تُرفض بكود `CARTONS_WITHOUT_KG` بدل افتراض المتبقي. |
| سقف حصة الأمر | لا يُسجَّل عبر البند فوق حصة أمره (`oi.Planned − oi.Produced`) — كود `OVER_ORDER`. |
| تحديث الحجز بعد الإنتاج (H1c) | `CloseProductionDay` كان يترك `ReservedQtyKg` المخزن مرتفعاً حتى الإقفال النهائي — فيُخفي المتاح عن الخطط (قاتل للخطط الفترية الطويلة). الآن يُعاد احتسابه بعد كل إقفال يوم عبر `RefreshLotReservations` (مستخرجة من `ApplyReservationsViaPlanning` دون تغيير منطقها). |
| ⚠ ملاحظة تشغيلية | تبيّن أثناء التنفيذ أن `ClosePlanItems` **بلا أي استدعاء من الواجهات** (الواجهة تستخدم `CloseProductionDay` + `ClosePlanFinal` فقط) — فتغييراته تحمي الـ API والاختبارات وتجهّز ربط الشاشة في B86. |

**تغييرات سلوكية مقصودة (حُدّثت اختباراتها):**
- `PlanLevelClosingTests` (الأول): `before + 1700` ← `before − 8300` (صرف 10,000 وعاد 1,700)؛ دخل البند الثاني أصبح صريحاً (6000 كجم/1200 كرتون) بدل (0/3000 المرفوضة الآن).
- `PlanLevelClosingTests` (جزئي): `16000` ← `6000` (صُرف 10,000 وعاد 6,000).
- `PlanClosureTests/T12`: `lotAfter > lotBefore` ← `lotBefore − 800` (صُرف 1,000 وعاد 200).

## 2) H2 — الخام المستهلك الفعلي (`OrdersWindows.cs` + `OrdersView.xaml.cs` + `ExecutionService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| حقل جديد في الحوار | `CloseDayDialog` يعرض «الخام المستهلك فعلياً (كجم)» — **الافتراضي = المتبقي المخطط** (سلوك اليوم عند عدم اللمس = كما قبل تماماً)، والمستخدم يعدّله بالفعلي. تحقق من السالب. |
| تمرير القيمة | موقعا الاستدعاء (`OrdersView` + بطاقة الأمر) يمرران `consumedRawKg: dlg.ConsumedKg` — كان المعامل الخلفي موجوداً ولا أحد يغذّيه. |
| تنبيه الماء | إن زاد الخارج عن الداخل تُلحق بالرسالة ملاحظة 💧 (ماء التشغيل غالباً) مع توجيه لتعريف معامل الإنتاجية — عرض فقط بلا رفض. |

## 3) H3 — معامل الإنتاجية لكل صنف (`BasicData.cs` + `MasterDataService.cs` + `ItemsView` + `ExecutionService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| عمود جديد | `Product.YieldFactor` من نوع `double?` (فارغ = غير معرَّف فلا يُحتسب انحراف). **آمن للترحيل:** `SchemaMigrator.FillNulls` يتخطى الأعمدة القابلة للفراغ — السجلات القديمة تبقى فارغة ولا يظهر لها أي تنبيه. |
| الحفظ | `SaveProductFull` + `SaveProduct` يقبلان `yieldFactor` اختيارياً (يُحفظ عند تمريره فقط — الفراغ يُبقي القديم). كل الاستدعاءات القائمة تعمل دون تعديل. |
| البطاقة | حقل «معامل الإنتاجية (خارج÷داخل — مثال 1.05)» في حقول التام بشاشة الأصناف (حفظ/عرض/تهيئة) مع رفض غير الموجب. |
| تنبيه الانحراف | عند الإقفال: المتوقع = الخارج ÷ المعامل مقابل الفعلي، والانحراف يُعرض (كجم + %) — وتجاوز ±5% يُبرز ⚠ مع توجيه للمراجعة. **تنبيه فقط، لا رفض أبداً.** |

## 4) H4 — إزالة المعدل الصامت 500 وتوحيد السلوك

| الملف | الإصلاح |
|---|---|
| `BasicData.cs` | الافتراضي `500 ← 0`. الصفر = «طاقة غير معرَّفة». **قواعد البيانات القائمة تحتفظ بقيم 500 المخزنة** — تُراجع من «الأصناف ← طاقات الأصناف» (البذور 500/1000 الصريحة لم تُمس). |
| `PlanningService.cs` | `EnsureSlotCapacity` تُرجع تنبيه ⚠ عند المعدل-صفر (يُلحق برسالة حفظ/تحديث الخطة، مكرراً مرة واحدة) — تُقبل مع التنبيه بدل التمرير الصامت. |
| `ProductionOrderService.cs` | حذف 4 بدائل `500` الصامتة (`CheckOrderCapacity` ×2 + `GetOrderSlot` ×2)؛ `reqHours` عند المعدل-صفر = 0 مع تنبيه (**موحّد مع مسار الخطة** بدل رفض ∞ السابق)؛ التنبيهات تُلحق برسائل الحفظ والاعتماد؛ إصلاح قسمة-على-صفر كامنة في تقدير زمن الانتهاء (`OrderCard`) كانت ستنفجر مع المعدل-صفر. |
| `IWorkflowServices.cs` | `OrderSlotInfo.CapacityNote` — التوزيع التلقائي لصنف بلا طاقة يجد صفراً متاحاً مع التنبيه بدل التوزيع على وهم. |
| اختبارات | لا تحديث لازم: الإيجابية تمر مع تنبيهات (لا يوجد `Assert.Equal` على نص رسالة في كامل الحزمة)، والسلبية (`CAPACITY_EXCEEDED`) مضبوطة على البذور الصريحة. |

## 5) H6 — فلتر الحالات في طاقة الورديات (4 استعلامات)

`ShiftUsageHours` (التخطيط) + `DayShiftUsedHours` (تقديم الخطة) + جزء الخطط في `CheckOrderCapacity` + جزء الخطط في `GetOrderSlot`:
استبعاد البنود المقفلة (`IsClosed`) + ربط (`Join`) مع الخطط لاستبعاد المقفلة/الملغاة (`Closed`/`Cancelled`/`IsClosed`) — بنفس نمط الربط المُثبت في `LotAvailableExcluding`.
كانت الخطط الملغاة والمقفلة تحجز الشفت **أبدياً**.

## 6) M7 + M9 + M3 (صفرية-الخطر)

| الكود | الإصلاح |
|---|---|
| M7 | تصحيح 4 رسائل/تعليقات تدّعي «صرف الخام عند الاعتماد» (الحقيقة: المواد المساعدة عند الاعتماد، والخام عند الإقفال): رسالة نجاح الاعتماد + زر الاعتماد + تأكيد الإلغاء + تلميح الخطوة التالية — مع 3 تعليقات اختبار. |
| M9 | `DocStatuses.PendingDelivery` ثابت معتمد + ترجمة «بانتظار التسليم» (كانت قيمة حرة تظهر إنجليزية) + توجيه موقع التعيين للثابت. |
| M3 | حذف بديل `7.5` الممنوع من نافذة الأمر الجديد: رفض صريح بأسماء الأصناف بلا وزن معرَّف + حراسة قسمة `PackWeight` من الصفر. |

## 7) ما أُجّل لـ B86 (بقرار)

| البند | السبب |
|---|---|
| H5/M8 محرك التوزيع العادل v2 | إعادة تصميم (منتج لكل عميل + حصة لكل صنف + ورديات/خطوط متعددة) — تحتاج قرارات تصميم من المستخدم قبل التنفيذ. |
| ربط `ClosePlanItems` بالواجهة | أصبحت دلالاته متسقة الآن (صرف + تحرير + حراس) — ربط شاشة إقفال البنود يحتاج تصميم UX. |
| H7/H8 وM1/M2/M4/M5/M6/M10–M13 | بنود متوسطة/صغيرة من التقرير — تُجدول بعد تثبيت العاجل. |
| مرتجع الدفعة المشتركة | بندَان من دفعة واحدة في استدعاء واحد: الصرف جُمّع (B85) لكن المرتجع ما زال لكل بند — قد يصطدم بمنع التكرار في حالة نادرة (تُعالج في B86 بتجميع المرتجع أيضاً). |

## 8) التحقق على Windows (أوامر)

```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests -c Release --filter "FullyQualifiedName~Closing|FullyQualifiedName~PlanLevel|FullyQualifiedName~PlanClosure|FullyQualifiedName~Capacity|FullyQualifiedName~Fair|FullyQualifiedName~Order"
dotnet test tests/DatesErp.Tests -c Release
```

**تحقق يدوي سريع:** (1) صنف تام جديد بلا طاقة ← حفظ خطة/أمر يعرض ⚠ ويقبل؛ (2) إقفال يوم يعرض حقل الخام المستهلك؛ (3) بطاقة الصنف تحفظ معامل 1.05 ويظهر 📊 عند الإقفال؛ (4) خطة ملغاة لا تحجز الشفت لخطة جديدة.
