# سجل إصلاحات B92 — خطط فترية لعدة عملاء بإنزال واختيار يدوي (2026-09-05)

## §1 — ما طُلب
خطط فترية (من–إلى) لعدة عملاء، لكن **الاختيار يدوي والإنزال يدوي بحسب ما تراه الإدارة** —
لا توزيع آلي ملزم: الإدارة تختار الدفعات والكميات وتوزعها على أيام الفترة والورديات بنفسها.

## §2 — ما كان ناقصاً (الفحص قبل التنفيذ)
- نافذة الاختيار (F6/F4) تدعم التحديد اليدوي والتاريخ لكل بند، لكن **الوردية قراءة فقط**:
  اليدوي كله يسقط على وردية الشاشة، والمحرك يفرض مجدولته بلا تجاوز.
- لا رؤية لتوزيع الأيام قبل الإنزال — الإدارة تُنزل عمياء ثم تكتشف التكدس.
- «✍️ إضافة يدوي»: بلا اختيار وردية، وتاريخه نص حر بلا التزام بالفترة.

## §3 — التنفيذ
- `PlanningWindows.cs`:
  - `ShiftOption` + `AllShifts` لكل بند + `ShiftId` متزامن الاسم — عمود «الوردية 🕐 *»
    كومبو لكل صف (خياراته بالساعات الفعالة)؛ الافتراضي وردية الشاشة، وبنود المحرك
    تبدأ بمجدولتها **مع جواز التجاوز اليدوي** — والوردية إلزامية عند الإنزال.
  - زر «📅 حمولة الأيام المحددة»: إجماليات البنود المعلّمة مجمعة بالتاريخ
    (البنود/الكراتين/الوزن/الورديات) قبل الإنزال.
- `PlanningView.xaml.cs`: موقعا الاستدعاء (اليدوي + العادل) يمرران الورديات النشطة؛
  «✍️ إضافة يدوي» أصبح بحقل وردية إلزامي + تاريخ يُتحقق أنه داخل فترة الخطة (قاعدة B80 نفسها).
- الحفظ: تواريخ البنود ووردياتها تُحفظ لكل بند (`ScheduledDate`/`SuggestedShiftId`) —
  خطة فترية متعددة العملاء يدوية بالكامل قابلة للاعتماد والفحص (B91) والتنفيذ.

## §4 — الملفات (3 + اختباران)
1. `.../PlanningWindows.cs` — الوردية اليدوية + حمولة الأيام.
2. `.../PlanningView.xaml.cs` — التمرير + اليدوي المكتمل.
3. `Services/BuildInfo.cs` — الختم B92.
4. `tests/.../PlanManualB92Tests.cs` — جديد (اختباران: ثبات التاريخ/الوردية لكل بند).
5. `tests/.../UiB92Tests.cs` — جديد (3 اختبارات بنيوية).

## §5 — التحقق على ويندوز (تسلسلياً)
```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~PlanManualB92Tests"
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~UiB92Tests"
```
فحص يدوي: خطة فترية متعددة العملاء → F6 → حدد بنوداً بتواريخ وورديات مختلفة →
«حمولة الأيام» → إنزال → حفظ → «فحص الخطة» يؤكد التوزيع اليدوي.
