# سجل إصلاحات B86 — من B85 إلى B86 (2026-09-05)

> النطاق: البنود الأهم أولاً من تقرير المراجعة — المحظورات التشغيلية وسلامة البيانات:
> H8 + H7 + M4a + M4b + M5 + M11 + M12 + تتمة H1 (مرتجع الدفعة المشتركة) + L2/L4/L5 + مقترح #16.
> المحرك العادل v2 (H5/M8) في B87 — يحتاج قرارين تشغيليين (أيام العمل/الورديات) سُئلا مع تسليم B86.
> التنفيذ عمياني (بلا مُصرّف/قاعدة هنا) — كل تغيير سلوكي موثق أدناه مع خطوات التحقق على Windows.

## 1) H8 — إصلاح عدّاد التسليم وإعادة الفتح (`FinishedGoodsService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| عدّاد على المستلَم | `alreadyDelivering` كان يجمع `NetWeightKg` بلا فلتر حالة — فالإلغاء (يُصفّر المستلَم ويُبقي المأمور) كان يُبقي الحصة محجوزة ويستحيل إعادة التسليم (`EXCEED_ORDER_QTY`). الآن: مجموع `ReceivedQtyKg` للسندات غير الملغاة — المسودات لا تحجب بعضها والإلغاء يحرر فوراً. |
| فحص السقف عند الاستلام | ثغرة المسودتين كانت تمرر تجاوزاً (لا فحص في `Receive`) — الآن كل استلام يُفحص مقابل (مستلَم السندات الأخرى + هذا السند) ≤ منتَج الأمر، مع مجمّع `recvAcc` لبندَي صنف واحد في السند نفسه. |
| سقف متعدد الدفعات | السقف = **مجموع** بنود الأمر لذات الصنف (كان أول بند فقط — طلب صنف واحد من دفعتين كان يُرفض خطأً). |
| إعادة الفتح التلقائي | إلغاء آخر سند كامل يعيد فتح الأمر **المغلق تلقائياً فقط** (`Completed` + مقفل ← مفتوح)؛ المغلق يدوياً (`Closed`) لا يُمس. |

## 2) H7 — كراتين البنود والمطابقة (`ExecutionService.cs` + `FinishedGoodsService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| توزيع الكراتين | `CloseProductionDay` يوزع `producedCartons` على البنود بالترتيب وبحدود مخطط كل بند (كان `ProducedCartons` البندي صفراً دائماً). و`ClosePlanItems` يسجل كراتين البند بسقف مخططه. إجمالي الجلسة (`ActualCartons`) محفوظ كما هو للفائض. |
| مطابقة سند التام | `SaveReceipt` ينبّه ⚠ عند تجاوز كراتين السند للمتبقي المنتَج (تنبيه لا رفض — إنتاج ما قبل B86 بلا كراتين مسجلة فالرفض سيكسر بيانات قائمة). |

## 3) M4a + M4b — تعديل البند (`PlanProgressService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| M4a: نهاية الكرتون-الزائف | إعادة الاشتقاق بلا وزن كرتون كانت تحسب `Ceiling(qty/0)` ← **كرتون واحد زائف** لأي كمية. الآن رفض صريح ⛔ بتوجيه لتعريف الوزن. |
| M4b: توحيد قاعدة الماء | التعديل فوق رصيد الدفعة كان مرفوضاً (والإنشاء مسموح) — الآن مسموح مع تنبيه ⚠ في رسالة النجاح، توحيداً مع الإنشاء. فحص الطاقة قبله لم يُمس (رفض السعة ما زال صارماً). |

## 4) M5 — المسلَّم لكل صنف (`ExecutionService.cs` + `CustomerDeliveryService.cs`)

`SyncDeliveredForCustomer` أصبح لكل (عميل + صنف + عبوة) بدل الكيلو الأعمى — كان تسليم خلاص يملأ فراغ سكري في تقدم البنود. التوقيع تغيّر (لا مستدعين في الاختبارات — المستدعي الوحيد حُدّث ليمرر بنود السند واحداً واحداً).

## 5) M11 — متبقي الكراتين (`ProductionOrderService.cs`)

`CheckPlanRemaining` يفحص الكراتين أيضاً (كود `OVER_PLAN_REMAINING` نفسه) — بضابطين ضد الرفض الكاذب: (1) عند **تطابق العبوة فقط** (أوزان مختلفة = أعداد غير قابلة للمقارنة)، (2) **سماح كرتونين** لخطأ التقريب عبر تقسيم الأوامر.

## 6) M12 + تتمة H1 — دقة المرتجع (`ExecutionService.cs`)

| الإصلاح | التفاصيل |
|---|---|
| M12: حصة المرتجع | التوزيع بنسبة **المصروف الفعلي** لكل دفعة (`takeByLot` من حلقة الصرف — نفس الوحدة فيجمع لـ1) بدل `المخطط/المستهلك` المخلوط، مع **سد كسور التقريب في آخر دفعة** وحركة واحدة للدفعة. الحالات أحادية الدفعة (كل اختبارات التكافؤ) أرقامها مطابقة حرفياً. |
| مرتجع الدفعة المشتركة | `ClosePlanItems` كان آخر موضع بحركة لكل بند — الآن المرتجع مجمّع لكل دفعة بحركة واحدة (كالصرف في B85). سجل البند (`ReturnedToRawKg`) محفوظ لكل بند — التجميع على مستوى الحركة المخزنية فقط. |

## 7) L2/L4/L5 + #16

| البند | الإصلاح |
|---|---|
| L2 | `GetOrderableItems.LotRemainingKg` = الرصيد − حجوزات الخطط الحية − الأوامر المستقلة الحية (بلا ازدواج: أمر الخطة داخل حصة خطته) — كان الرصيد الخام المتفائل. |
| L4 | صياغة صادقة: «أُعيد لخام دفعته — أعد تخطيطه يدوياً» بدل «رُحِّل لخطة اليوم التالي» (لا أتمتة) — في الرسالة وتسمية الخيار معاً. |
| L5 | رسالة الفحص المفقود توضح البديلين: إقفال مع «إرسال للفحص» أو فحص مستقل من شاشة الجودة. |
| #16 | تنبيه 📅 عند حفظ/تحديث خطة > 7 أيام: مراجعة الإشغال الأسبوعي وعدم التكديس. |

## 8) إصلاح انحدار B85 (مُكتشف ذاتياً)

اختبار `FairDistributionTests` (تغيير الصنف) كان يعتمد على الافتراضي 500 المزال في B85/H4 (منتج بقدرات فارغة ومعدل-صفر كان سيجعل رفض الطاقة المتوقع يمر!). أُعطي المنتج البديل طاقة صريحة (3000 كرتون/وردية 2 = 500/س) فبقيت نية الاختبار (رفض 8 ساعات > 6) سليمة.

## 9) ما أُجّل لـ B87 (بقرار)

| البند | السبب |
|---|---|
| H5/M8 المحرك العادل v2 + M6 توحيد العميل | يحتاج قرارين: أيام العمل (تخطي الجمعة؟) والورديات (واحدة يختارها المستخدم أم كل النشطة؟) — سُئلا مع التسليم. |
| M10 نافذة التوقفات | عمل واجهة (شبكة ساعات/سبب داخل حوار الإقفال) — مستقل وواضح. |
| M13 أحادية/تعدد الأصناف | يحتاج قرار: فرض صنف واحد للأمر أم دعم كامل متعدد (إقفال/جودة لكل بند)؟ — سُئل مع التسليم. |
| L1 تقييد الأوامر اليدوية | يحتاج قرار صلاحيات — مقبول إجرائياً حالياً. |
| L3 عداد الأيام الصفرية | يُعالج ضمن المحرك v2 (عدم احتساب أيام الحصة-صفر). |
| صرف الدفعة المشتركة (يومي) | حلقة الصرف تنشر لكل بند — بندان لذات الدفعة في أمر واحد قد يصطدمان بمنع التكرار (نادر: الأوامر الطبيعية بند-للدفعة). يُجمّع في B87 مع مراجعة الصرف. |

## 10) التحقق على Windows (أوامر)

```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests -c Release --filter "FullyQualifiedName~Fair|FullyQualifiedName~Finish|FullyQualifiedName~Deliver|FullyQualifiedName~Order|FullyQualifiedName~Plan|FullyQualifiedName~Closing|FullyQualifiedName~Balance|FullyQualifiedName~Ledger"
dotnet test tests/DatesErp.Tests -c Release
```

**تحقق يدوي سريع:** (1) إلغاء سند تام كامل ← إعادة التسليم بسند جديد تنجح والأمر يُعاد فتحه؛ (2) إقفال يوم ← كراتين البنود مسجلة؛ (3) سند تام بكراتين زائدة ← تنبيه ⚠؛ (4) تعديل بند فوق رصيد الدفعة ← يقبل مع تنبيه.
