# سجل الإصلاحات — الدفعة B106 (الإصدار 1.50.0)

**الأساس:** B105 (`ganaasalah881-dev/RR`) — وهو **كودي B101 + 16 ملف طبقة المهام ومعالجة الخام**، أي المصدر الحقيقي للنسخة B103 التي يشغّلها المستخدم.
**الختم بعد التحديث:** `2026-09-07 B106`

---

## §0 — تغيير هيكلي: الكود المصدري في جذر المستودع

اعتُمدت شجرة `src/ tests/ tools/ audit/ Documentation/ Installer/ .github/` في جذر المستودع **بدل حزم ZIP**.

**السبب:** ملف `.github/workflows/ci.yml` كان موجوداً منذ B83 لكنه **لم يعمل ولا مرة** — لأن المستودع لم يحوِ سوى ملفات ZIP لا شجرة كود، فلا شيء تُشغّله GitHub Actions عليه. نتيجةً لذلك سُلِّمت **18 حزمة متتالية (B84→B101) دون أن تُترجَم أو تُختبر ولو مرة واحدة** (موثّق حرفياً في FIXLOG B98/B99/B100/B101)، وشهد الطرف الآخر بأن الكود «لم يكن يُترجَم» واحتاج ~12 إصلاح ترجمة.

هذا التغيير يُنهي المشكلة من جذرها: كل دفعة قادمة تُترجَم وتُختبر آلياً قبل التسليم.

---

## §1 — 🖨️ معاينة الطباعة: صفحات فارغة · نموذج ممطوط · مظهر غير احترافي

### السبب الجذري (سطر واحد)
بُناة النماذج كلها تضبط مقاسها بشكل صحيح:
```csharp
PageWidth = 1122, PageHeight = 794,   // A4 أفقي
ColumnWidth = double.MaxValue,        // عمود واحد يملأ الصفحة
```
ثم كانت `PrintPreviewWindow` تدهس هذا الضبط في السطر 30 ولا تُعيده أبداً:
```csharp
_doc.ColumnWidth = 1000;   // ← أصل الأعراض الثلاثة
```

| العَرَض | التفسير |
|---|---|
| **صفحات فارغة** | النموذج الرأسي: 794 عرضاً − 80 هوامش = **714 متاح**، والعمود صار **1000** — أعرض من الصفحة، فيعجز المُرقِّم عن الرصف ويُخرج صفحات بيضاء. |
| **«شاشة أفقية والنموذج رأسي»** | `FlowDocumentScrollViewer` **يتجاهل `PageWidth/PageHeight` كلياً** ويعرض شريطاً متصلاً؛ والنافذة `Maximized` فيُمطّ النموذج الرأسي داخلها. |
| **غير احترافي** | للسبب نفسه: بلا حدود صفحة ولا فواصل صفحات ولا شكل A4. |
| **اتجاه خاطئ** | `PrintDocument` كان يُستدعى بلا ضبط `PrintTicket`، فالنموذج الأفقي يُرسل إلى ورقة رأسية. |

### الإصلاح
| الملف | التعديل |
|---|---|
| `Views/PrintPreviewWindow.xaml` | `FlowDocumentScrollViewer` ← **`FlowDocumentPageViewer`**: عارض صفحات حقيقي يحترم مقاس المستند ويعرض صفحات A4 منفصلة بتنقّل بينها. |
| `Views/PrintPreviewWindow.xaml.cs` | `ColumnWidth = 1000` ← **`double.MaxValue`** (عمود واحد — نيّة البُناة الأصلية). |
| `Views/PrintPreviewWindow.xaml.cs` | `Print_Click`: ضبط **`PrintTicket.PageOrientation`** من أبعاد المستند (أفقي إن `PageWidth > PageHeight`)، داخل `try/catch` لأن بعض برامج التشغيل ترفض التذكرة. |
| `Views/PrintPreviewWindow.xaml.cs` | تثبيت **`paginator.PageSize`** على أبعاد المستند قبل الترقيم (لا مقاس العارض). |

**الأثر:** يشمل **كل** مطبوعات النظام — فكل الشاشات تمر عبر هذه النافذة الواحدة (`ExportPrintService`, `PlanningView`, `ReceivingView`, `DeliveryView`, `OrdersView`, `QualityView`, `CartonView`, `FinishedGoodsView`).

---

## §2 — 📦 أمر الإنتاج: لا يحفظ · والاعتماد يرفض

### السبب الجذري: فلتران متناقضان لنفس الاختيار
في `OrdersWindows.cs`:
```csharp
السطر 262:  r.IsChecked && r.Cartons > 0                     // ← يشترط الكراتين
السطر 314:  r.IsChecked && (r.QtyKg > 0 || r.Cartons > 0)    // ← يقبل الكيلو أيضاً
```

**تسلسل العطل:** تؤشّر المربع وتُدخل الكمية **بالكيلو** ← السطر 262 **يُسقط الصف صامتاً** ← الأمر يُحفظ **بلا بنود** ← `ApproveOrder` يرفض: «لا يمكن اعتماد أمر بدون بنود».

فـ«لا يحفظ» و«لا يوافق» **عَرَضان لعلة واحدة**، لا مشكلتان.

### الإصلاح
وُحِّد الشرطان على `(r.Cartons > 0 || r.QtyKg > 0)`.

---

## §3 — 🚚 التسليم للعميل: النموذج صفر · لا أصناف · لا كميات

### السبب الجذري
`FinishedGoodsService.cs` — لاستلام مخزن التام مساران:
```csharp
السطر  83:  int? effCust = null;
السطر 102:  effCust = line.CustomerId;   // ✅ مسار «المربوط بأمر تسليم» فقط
            // ❌ المسار المباشر لا يضبطه إطلاقاً
السطر 159:  CustomerId = effCust,        // ← يُكتب في المخزون
```
فتدخل البضاعة مخزن التام **بلا هوية عميل** (`CustomerId = NULL`).

و`DeliveryView` تبحث هكذا:
```csharp
.Where(b => b.WarehouseId == whFg && b.CustomerId == cust.Id && ...)
```
و`NULL == cust.Id` **لا يطابق شيئاً أبداً**.

### لماذا تنهار الشاشة بالكامل؟
| ما يراه المستخدم | السبب |
|---|---|
| لا تظهر كميات العميل | الاستعلام يرجع صفر صفوف ← `BalanceGrid` فارغ |
| **لا يوجد اختيار للصنف** | لا توجد قائمة أصناف أصلاً — الاختيار **بالنقر المزدوج على جدول الرصيد**. الجدول فارغ ← لا شيء يُنقر |
| نموذج بدون كميات | الكميات تُنسخ من الصف المنقور |
| «النموذج صفر» | `BalanceChip` يعرض «رصيد العميل: 0.0 كجم» رغم وجود البضاعة فعلاً |

**الشاشة ليست ناقصة التصميم — مصدر بياناتها مقطوع.**

### الإصلاح (شقّان)
1. **الكود** — في المسار المباشر تُشتق الهوية من ملكية سطر الأمر ثم من عميل الأمر:
   ```csharp
   effCust = orderItem.CustomerId ?? order.CustomerId;
   ```
2. **البيانات القائمة** — إصلاح الكود لا يُصلح ما سُجِّل سابقاً. أُضيف:
   `tools/SqlFixes/B106_إصلاح_هوية_العميل_في_مخزن_التام.sql`
   بأربعة أقسام: تشخيص (قراءة فقط) ← استنتاج العميل لكل صف ← تصحيح داخل `TRANSACTION` مع **دمج الكميات** عند وجود صف مقابل ← تحقق نهائي. الصفوف التي تعذّر استنتاج عميلها **لا تُمَس**.

---

## §4 — ⏳ معلّق بانتظار قرار المستخدم

| البند | الحالة |
|---|---|
| **تقسيم خطط الإنتاج** | السلوك الحالي (`OrdersWindows.cs:351`) — أمر لكل (عميل × يوم مجدول × وردية) — **مقصود وموثّق** في `PRODUCTION_PLANNING_REVIEW_AR.md:62`. تغييره يحتاج تحديد البديل: أمر واحد للخطة / لكل عميل / لكل يوم. |
| **شاشة الفحص (QualityView)** | تأكّد آلياً أن `QualityView.xaml` **مطابق حرفياً** من B96 حتى B105 — لم يُطوَّر عبر 9 نسخ. إعادة تطويرها تحتاج مواصفات المستخدم. |

---

## §5 — الملفات المتغيرة

| الملف | التغيير |
|---|---|
| `src/DatesErp.Desktop/Views/PrintPreviewWindow.xaml` | عارض الصفحات |
| `src/DatesErp.Desktop/Views/PrintPreviewWindow.xaml.cs` | العمود + الاتجاه + مقاس الترقيم |
| `src/DatesErp.Desktop/Views/Screens/OrdersWindows.cs` | توحيد فلتر الاختيار |
| `src/DatesErp.Application/Services/FinishedGoodsService.cs` | اشتقاق هوية العميل |
| `src/DatesErp.Desktop/Services/BuildInfo.cs` | الختم B106 |
| `src/DatesErp.Desktop/DatesErp.Desktop.csproj` | الإصدار 1.50.0 |
| `tools/SqlFixes/B106_…​.sql` | **جديد** — تصحيح البيانات |

---

## §6 — ⚠️ التحقق المطلوب منك

**لا يوجد .NET SDK في بيئة العمل هذه، فلم يُنفَّذ بناء ولا اختبار.** ما جرى:
- ✅ فحص توازن الأقواس في الملفات الثلاثة المعدّلة — سليم
- ✅ التحقق من صحة XML لملف XAML المعدّل — سليم
- ✅ التحقق من أن كل استخدامات `Viewer` (`Document`/`Zoom`/`ActualWidth`) متوفرة في `FlowDocumentPageViewer`
- ✅ التحقق من وجود `ProductionOrderItem.CustomerId` و`ProductionOrder.CustomerId`

**لكن هذا لا يغني عن الترجمة.** والآن — بعد اعتماد الشجرة في §0 — سيتولى `ci.yml` ذلك آلياً على GitHub Actions.

للتشغيل محلياً على ويندوز:
```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --no-build
```
**بعد التحديث يجب أن يظهر في عنوان النافذة: `2026-09-07 B106`**

---

## §7 — إصلاح خط التكامل المستمر (CI) نفسه

عند اعتماد الشجرة تبيّن أن `ci.yml` **ما كان ليعمل حتى لو وُضع في مستودع صحيح** — ثلاثة عيوب بنيوية تؤكد أنه لم يُشغَّل ولا مرة منذ كتابته في B83:

| # | العيب | الأثر | الإصلاح |
|---|---|---|---|
| 1 | حزمة B105 وضعته في **`.github/ci.yml`** بدل `.github/workflows/ci.yml` | GitHub لا يراه إطلاقاً — لا وظيفة تُنشأ | نسخة مصححة في `tools/ci/ci.yml` ⚠️ تحتاج نقلة يدوية |
| 2 | `build-and-test` على **`ubuntu-latest`** بينما `DatesErp.Desktop` هدفه `net8.0-windows` مع `UseWPF` | **البناء يفشل فوراً** — لا يمكن بناء WPF على لينكس | `runs-on: windows-latest` + `shell: bash` للخطوات النصية |
| 3 | خطوة «اختبارات القبول» داخل وظيفة **لا تُثبّت .NET ولا تبني شيئاً**، ثم تستدعي `--no-build` | فشل حتمي | أُضيف `setup-dotnet` وحُذف `--no-build` |

`AcceptanceRunner` هدفه `net8.0` ويعتمد على `Application`/`Infrastructure` فقط (لا `Desktop`) — فيبقى على لينكس بلا مشكلة.

### ⚠️ خطوة يدوية مطلوبة منك
صلاحيات التطبيق **لا تسمح بكتابة ملفات `.github/workflows/`** (قيد أمني في GitHub)، فوُضعت النسخة المصححة في **`tools/ci/ci.yml`**.
لتفعيلها:
```bash
mkdir -p .github/workflows
git mv tools/ci/ci.yml .github/workflows/ci.yml
git rm .github/ci.yml
git commit -m "تفعيل CI" && git push
```
التفاصيل في `tools/ci/README_تفعيل_CI.md`.

**النتيجة بعد التفعيل:** أول تشغيل حقيقي لـCI في تاريخ المشروع — وأول ترجمة فعلية للكود منذ B83.
**توقّع فشل أول تشغيل**: سيكشف أخطاء الترجمة المتراكمة منذ B84 دفعةً واحدة بنصوص دقيقة — أرسلها لي وأعالجها.

---

## §8 — الواجهة: أزرار الشريط العلوي وزر البحث

### ٨/١ تفاوت عرض الأزرار ونزولها سطراً ثانياً
**السبب:** نمط `ErpButton` في `DateErpTheme.xaml` يضبط `Padding` و`Margin` و`MinHeight` — **ولا يضبط `MinWidth` إطلاقاً**. فكل زر يأخذ عرض نصه فقط: «حذف» يظهر قزماً بجانب «إلغاء الاعتماد». والحاوية `ErpToolbar : WrapPanel` تُنزل الفائض سطراً ثانياً — وهو سلوك مقصود لمنع خروج الأزرار عن النطاق، لكنه يقع مبكراً لأن التسميات طويلة بلا داعٍ.

**الإصلاح (شقّان):**
1. **`MinWidth = 92`** + `HorizontalContentAlignment = Center` في `AddBtn` — يُسوّي القِصار دون قصّ الطوال.
2. **تقصير التسميات**: اختصارات لوحة المفاتيح انتقلت من نص الزر إلى **التلميح**:

| قبل | بعد | التلميح |
|---|---|---|
| `➕ جديد (F2)` | `➕ جديد` | جديد (F2) |
| `💾 حفظ (F10)` | `💾 حفظ` | حفظ (F10) — المستند يبقى مفتوحاً أمامك بعد الحفظ |
| `✏️ تعديل (F3)` | `✏️ تعديل` | تعديل (F3) |
| `🗑️ حذف (F8)` | `🗑️ حذف` | حذف (F8) |
| `🔍 بحث (F9)` | `🔍 بحث` | بحث (F9) |
| `🔄 تحديث (F5)` | `🔄 تحديث` | تحديث (F5) |
| `🖨️ طباعة (Ctrl+P)` | `🖨️ طباعة` | طباعة (Ctrl+P) |
| `🔓 إلغاء الاعتماد` | `🔓 فكّ الاعتماد` | إلغاء اعتماد المستند وإعادته للتعديل |

النتيجة: الشريط أقصر بنحو الثلث ← نزول السطر الثاني يقع في حالات نادرة فقط، والأزرار متساوية بصرياً.

### ٨/٢ «قصة زر البحث» — نفس الزر يفعل أشياء مختلفة
**السبب:** زر `🔍 بحث` واحد في الشكل، لكن سلوكه يختلف جذرياً حسب الشاشة:

| الشاشة | ما كان يفعله فعلاً |
|---|---|
| `DeliveryView` · `FGReceiveView` · `FinishedGoodsView` · `ProductionDeliveryView` · `ShiftsView` | **`RefreshList()` فقط — أي نفس زر «تحديث» حرفياً!** زران مختلفا الاسم بنفس الأثر. |
| `QualityView` · `ReceivingView` | `OpenSearchWindow()` — نافذة بحث حقيقية |
| `PlanningView` | تحديث + تركيز صندوق البحث |

**الإصلاح:** وُحِّد السلوك على نمط `PlanningView` المُثبت — «بحث» = تحديث القائمة **ثم تركيز صندوق البحث** في الشاشة (أربع شاشات تملك الصندوق أصلاً: `DelivSearchBox` / `RecSearchBox`). فصار للزر معنى مستقل عن «تحديث».
`ShiftsView` تُركت (شاشة تعريف صغيرة بلا صندوق بحث)، و`QualityView`/`ReceivingView` تُركتا (نافذة بحث حقيقية أقوى).

### ٨/٣ «احفظ وسيظل في الشاشة»
ليست تسمية زر — بل **سلوك مقصود وموثّق** (`DeliveryView.xaml.cs:262` «§4/§5 — الحفظ ينجح ويبقى السند مفتوحاً»)، ورسالته: «السند باقٍ أمامك — اعتمده لخصم الكميات». الغرض أن تعتمد بعد الحفظ مباشرة دون إعادة فتح.
**ما تغيّر:** التلميح على زر الحفظ صار يقولها صراحةً بدل أن يفاجأ بها المستخدم.
**إن أردت تغيير السلوك نفسه** (إغلاق النموذج بعد الحفظ / مسحه للإدخال التالي) فهو قرارك — أخبرني.

### ٨/٤ «التسليم للفحص» — ⏳ بانتظار تحديدك
لا توجد شاشة بهذا الاسم حرفياً. المرشح الأقوى **«معالجة وتعقيم الخام» (`treatment` · `RawTreatmentView`)** — وهي من الشاشات الستَّ عشرة التي أضافها الخط الآخر ولم أبنِها أنا، ولم يُفحص أيٌّ منها ضد دورة عمل حقيقية.
الطلب المفهوم: **إلغاء الشاشة المنفردة** وجعلها **خياراً داخل «الاستلام وسندات الاستلام»** — وجهة الكمية: مخزن الخام أم مخزن الفحص/المعالجة — ثم تستكمل الدورة.
هذا تغيير معماري (كيانات + خدمة + شاشتان)، فلا يُنفَّذ بالتخمين.

---

## §9 — دورة معالجة وتعقيم الخام: فحص ضد دورة العمل الحقيقية

### ما وجدته فعلاً (أفضل مما توقّعت)
| البند | الحالة |
|---|---|
| مستودع `WTRT` «مستودع المعالجة والتعقيم» | ✅ **مخزن حقيقي مبذور** — لا وهمي |
| حركة المخزون | ✅ عند البدء: خام ← معالجة · عند الإفراج: معالجة ← خام |
| الإفراج الجزئي · الرفض · الإلغاء | ✅ موجودة |
| موعد الجاهزية + رصد المتأخرات | ✅ `ExpectedReadyAt` محسوب ومخزَّن + `IsOverdue` |
| حارس زمني | ✅ لا إفراج قبل اكتمال المدة، والإفراج **فعل بشري** لا مؤقّت خلفي |

### الفجوات مقابل الدورة التي وصفتَها

| # | متطلبك | الحالة قبل B106 |
|---|---|---|
| ١ | المدة **حسب درجة الإصابة**: 5 / 7 / 10 أيام | ❌ المدة تتبع **تقنية المعالجة** لا الإصابة. المبذور: حراري 6س · تجميد 168س · تبخير 72س. **لا 5 أيام ولا 10 أيام، ولا حقل إصابة أصلاً** |
| ٢ | الخروج **لمخزن التام أو لخط الإنتاج** | ❌ الإفراج يعود **لمخزن الخام إجباراً** — مثبَّت في الكود بلا خيار |
| ٣ | خيار الوجهة **داخل شاشة الاستلام** | ❌ **لا رابط إطلاقاً** بين `ReceivingService`/`ReceivingView` والمعالجة — صفر إشارة |
| ٤ | لا شاشة منفردة | ❌ شاشة مستقلة تُلزم الموظف بإعادة اختيار الدفعة والصنف والكمية يدوياً |

**حكمك صحيح:** الفجوة ①+③ هي بالضبط سبب «لن يستطيع الموظف أن يعمل عليه» — يستلم الشحنة في شاشة، ثم يذهب لشاشة أخرى ويُعيد إدخال كل شيء من الذاكرة، وبمدة لا تمثّل حالة البضاعة.

### ما نُفِّذ في B106

**① المدد حسب درجة الإصابة** — `DbSeeder.cs`
```
TRT-INF-L   إصابة خفيفة — 5 أيام    120 ساعة
TRT-INF-M   إصابة متوسطة — 7 أيام   168 ساعة
TRT-INF-H   إصابة شديدة — 10 أيام   240 ساعة
```
تُبذر **صفاً صفاً بنمط «إن لم يوجد»** لا داخل `!Any()` — وإلا لما وصلت قاعدتك القائمة. وكلها `RequiresQualityCheck = true`: بضاعة كانت مصابة لا تخرج بلا فحص معتمد.

**② وجهة الإفراج قابلة للاختيار** — `RawTreatmentService.Release(...)`
أُضيف `destWarehouseCode` (افتراضه `"WRM"` فلا يتغير أي سلوك قائم):
- `"WRM"` مخزن الخام · `"WFG"` **مخزن التام مباشرة**
- الوجهة محصورة في مخزنين معلومين — لا كود حر يمر إلى حركة مخزون
- **تصحيح محاسبي مصاحب:** `lot.TreatmentReadyQtyKg` (رصيد الخام الجاهز للتصنيع) لم يعد يزيد عند الخروج إلى التام — كان سيحتسب الكمية مرتين
- الواجهة `IRawTreatmentService` حُدِّثت بمعامل اختياري ← كل المستدعين القائمين يظلون يترجمون

### ما لم يُنفَّذ بعد (يحتاج قرارك)
| البند | السؤال المفتوح |
|---|---|
| **③ خيار الوجهة عند الاستلام** | هل الاختيار **لكل سند** أم **لكل بند/دفعة**؟ ومن يحدد درجة الإصابة — أمين المخزن عند الاستلام أم الجودة؟ |
| **④ إلغاء الشاشة المنفردة** | تُلغى تماماً، أم تبقى **لوحة متابعة** (المعالجات الجارية/المتأخرة/الإفراج) بينما البدء يصير من الاستلام؟ |
| **الصرف المباشر لخط الإنتاج** | هذا ليس حركة مخزن بل **صرف مواد لأمر إنتاج**. أيُصرف من `WTRT` مباشرة لأمر قائم، أم يمر بالخام أولاً؟ |

---

## §10 — تدقيق المواصفة الكاملة لدورة المعالجة (٧ بنود) على الكود القائم

**سؤالك: «هل تم إعادة تصميمه بهذا التفصيل؟» — الجواب: نعم، بنسبة تفوق التوقع بكثير.** أدناه التدقيق بنداً بنداً بالدليل من الكود.

### ①  المستودعات والحالات — ✅ منفَّذ
| المطلوب | الدليل |
|---|---|
| مستودع مستقل | `WTRT` «مستودع المعالجة والتعقيم» مبذور في `DbSeeder` |
| تمييز الحالات الأربع | `TreatmentStatuses`: InProgress · Released · Rejected · Cancelled |
| حقول الدفعة | `InStockQtyKg` · `UnderTreatmentQtyKg` · `TreatmentReadyQtyKg` · `ReservedQtyKg` |
| **تحت المعالجة ليست متاحة** | `Lot.AvailableQtyKg => InStockQtyKg − ReservedQtyKg − UnderTreatmentQtyKg` |
| ثابت التوازن | موثّق في الكيان: **رصيد(WRM) + رصيد(WTRT) = InStockQtyKg** |
| صنف يشترط معالجة | علم `Product.RequiresTreatment` |

### ②  تسجيل العملية والتقسيم — ✅ منفَّذ
- **التقسيم بلا صنف جديد:** عدة صفوف `RawTreatment` على نفس `LotId` — مُختبر حرفياً بـ500+500.
- **كل الحقول المطلوبة موجودة:** `TreatmentNo` · `LotId` · `ProductId` · `TreatmentTypeId` · `QtyKg` · `PackageCount` · `StartedAt` · `DurationHours` · `ExpectedReadyAt` · `CompletedAt` · `ResponsibleUserId` · `Notes` · `Status`.
- **الجاهزية تُحسب تلقائياً** = `StartedAt + DurationHours`، وتُخزَّن لا تُشتق (فلا تتغير لو عُدّلت مدة النوع لاحقاً).
- 🆕 **B106:** المدد حسب درجة الإصابة 5/7/10 أيام.

### ③  حركة المخزون — ✅ منفَّذ
`WRM → WTRT` عند البدء · `WTRT → WRM` عند الإفراج · حارس زمني يمنع الإفراج قبل اكتمال المدة · الإفراج **فعل بشري** لا مؤقّت خلفي.
🆕 **B106:** وجهة الإفراج صارت اختياراً (خام أو **تام مباشرة**).

### ④  خطط الإنتاج حسب التاريخ — ✅ منفَّذ
`PlanningService.CheckTreatmentReadiness(plan)` — **مستدعاة فعلاً عند الحفظ/الاعتماد** (سطر 597).
تحسب لكل بند: `TreatmentReadyQtyKg + ما ينضج حتى تاريخ ذلك البند − المحجوز لغيره − المنتَج`، وترفض برسالة تذكر **الرقم والسبب وأقرب موعد ممكن**.
والحجز يطرح `planCommitted + orderCommitted` فلا تستخدم خطتان نفس الكمية.

### ⑤  المعالجة الجزئية — ✅ منفَّذ
`Release(treatmentId, qtyKg)` جزئي، والباقي يبقى تحت المعالجة برقم مرجعي فريد لكل إفراج (`{TreatmentNo}/R{n}`) كي لا يرفضه حارس التكرار.

### ⑥  التتبع والتقارير — ✅ منفَّذ
`ReportServiceTreatment.cs` + `TreatmentReportsTests.cs` + تقرير المتأخرات.

### ⑦  الاختبارات التنفيذية — ✅ مكتوبة ❌ لم تُشغَّل
**سيناريوك الحرفي مكتوب اختباراً:** `RawTreatmentScenarioTests` — 5,000 سلة · 4,000 جاهزة · 500 لسبعة أيام · 500 لعشرة. **١٣ اختباراً** تغطي بنودك السبعة:

| اختبارك | الاختبار المقابل |
|---|---|
| ١ لا استخدام لما تحت المعالجة | `P1_UnderTreatment_Is_Not_Available_For_Production` |
| ٢ بعد ٧ أيام تجهز الأولى | `P2_P3_Release_Blocked_Before_Duration_Then_Allowed` |
| ٣ بعد ١٠ أيام تجهز الثانية | `P3_Second_Batch_Ready_After_Ten_Days` |
| ٤ الخطة تحسب حسب التاريخ | `P4_Available_Depends_On_Production_Date` (0 في اليوم ٣ · 500 في ٨ · 1000 في ١١) |
| ٥ الحجز يمنع الازدواج | `P5_Reservation_Prevents_Double_Use_And_Blocks_Treating_Reserved` |
| ٦ المخزون يطابق بلا نقص | `P6_Warehouse_Balances_Always_Equal_Lot_Stock` |
| ٧ التتبع الكامل | `P7_Full_Traceability_Chain_Is_Intact` |

**لكن — ولا اختبار من الثلاثة عشر شُغِّل ولو مرة.** لا SDK في بيئة البناء ولا CI عامل. فهي «مكتوبة» لا «ناجحة».

---

## الفجوات الحقيقية المتبقية

| # | الفجوة | الحجم |
|---|---|---|
| ١ | **لا رابط من شاشة الاستلام** — بندك ② يقول «عند استلام شحنة خام يمكن تقسيم الكمية»، والواقع: **صفر إشارة** بين `ReceivingService`/`ReceivingView` والمعالجة. التقسيم ممكن في المحرك لكن **لا مدخل له من حيث يعمل الموظف**. | متوسط |
| ٢ | **الشاشة المنفردة** — قرارك: تُلغى. الأنسب تقنياً: يُنقل **البدء** إلى الاستلام، وتبقى الشاشة **لوحة متابعة** (الجارية · المتأخرة · الإفراج) — فالموظف يحتاج مكاناً يرى فيه ما تحت المعالجة اليوم. | متوسط |
| ٣ | **ازدواج منطق التوفّر بالتاريخ** — نسختان: `RawTreatmentService.GetAvailableForDate` (تستدعيها الاختبارات فقط) و`PlanningService.CheckTreatmentReadiness` (الفعّالة). منطقان متوازيان لقاعدة واحدة = خطر انحراف. يجب توحيدهما. | صغير |
| ٤ | **لم يُترجم ولم يُشغَّل قط** — وهذه أخطر الأربع. | يحلّها CI |

**الخلاصة:** المحرك مبنيّ بعمق يطابق مواصفتك، والناقص هو **الوصلة إلى الموظف** (①②) و**التحقق الفعلي** (④). لهذا شعرتَ أن «الموظف لن يستطيع العمل عليه» — المحرك سليم والمقود غير موصول.

---

## §11 — شاشة الفحص والجودة: التدقيق والتصحيح الأول

### ما وجدته — الشاشة ليست فارغة كما بدا
`QualityView` (214 سطر XAML + 1,167 سطر C#) تحوي فعلاً:
- **بطاقة ترويسة كاملة**: أمر الإنتاج · الخطة · العميل · تاريخ الإنتاج · دفعة الخام · المنتج التام · الكمية · الوحدة · الوردية · الخط
- **ثلاثة مصادر للفحص**: أمر إنتاج · جلسة تشغيل · يدوي
- **ثلاثة قرارات**: 🟢 مطابق · 🟡 حجز · 🔴 مرفوض
- **شريط إجماليات**: المفحوص · المقبول · المرفوض · حسب الصنف · الخسارة · النسبة
- وخلفها `InspectionService` غنية: أنواع نتائج قابلة للتعريف · ملفات لكل صنف/مجموعة · تحويل وحدات · `ValidateAgainstProduction` · `ComputeGradeSummary`

**فالمشكلة ليست غياب الوظائف — بل أن الواجهة لم تُراجَع منذ B96** بينما تطوّرت الخدمات خلفها (B95 المرفوض النهائي · B99 دورة الجودة و`QCWindow`).

### التصحيح المنفَّذ: تناقض «منسم»
قاعدة المصنع مطبَّقة في البذر: النوع `RT-MONSAM` «تمر منسم» مُعرَّف
`ResultKind = KindAccepted` + `IsFinishedGood = true` + `EntersInventory = true`
— أي **منتج تام مقبول قابل للبيع والتسليم**، وتعليق البذرة يقولها صراحةً: «المنسم ليس مخرجاً ثانوياً».

**ومع ذلك** كانت تسمية الدرجة الرافضة:
```csharp
GradeNonConforming => "غير مطابق / منسم"
```
أي أن مسؤول الجودة يرى المنسم مقترناً بالرفض، **عكس التصنيف المعتمد تماماً**.

**ملاحظة على الخط الآخر:** B103 أعلنت إصلاح «ترويسة منسم المثبتة في شاشة الجودة» — وبمقارنة نصوص الثنائيات تبيّن أنها صحّحت **الترويسة المولَّدة** (`غير مطابق (` بدل `غير مطابق / منسم (`) **وتركت التسمية الثابتة**. فالإصلاح كان نصفياً، واكتمل هنا:

| الموضع | قبل | بعد |
|---|---|---|
| `Inspection.GradeNameAr` | `غير مطابق / منسم` | `غير مطابق` |
| `QualitySetupWindow` (نص خيار) | `... وإلا فغير مطابق / منسم` | `... وإلا فغير مطابق` |

صفر أثر متبقٍ في `src/`.

### ما تبقّى — يحتاج تحديدك
إعادة بناء الشاشة (1,167 سطراً) على كود **لم يُترجَم بعد** تكرارٌ لخطأ الـ18 حزمة. وبعد بناء الغد أنفّذها فوراً — لكني أحتاج منك تحديد الخلل كما تراه أنت:
1. **ما الذي لا تستطيع فعله فيها اليوم؟** (خطوة ناقصة · حقل غير موجود · قرار لا يمكن تسجيله)
2. **من يفتحها ومتى؟** مسؤول الجودة بعد إقفال يوم الإنتاج، أم عند كل جلسة تشغيل؟
3. **علاقتها بـ`QCWindow`** (نافذة محضر الفحص من بطاقة «إنتاج جاهز للفحص» — B99): أتُدمجان أم لكلٍّ دوره؟
