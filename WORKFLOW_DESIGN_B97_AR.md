# B97 (تصميم) — النظام المُوَجَّه بالمهام Workflow-Driven: الموظف لا يبحث عن عمله، النظام يعرض له عمله

**تاريخ الوثيقة:** 2026-09-06
**الحالة:** قيد اعتماد الإدارة — لا تنفيذ قبل الاعتماد
**النطاق:** وثيقة تصميم فقط. لا برمجة، لا تعديل قاعدة بيانات، لا تعديل شاشات، لا API جديد، لا حذف أي وظيفة موجودة.
**القاعدة الحاكمة:** كل موظف عند دخوله النظام يعرف فوراً: ماذا لدي؟ ماذا يجب أن أفعل؟ من أرسلها لي؟ ما الذي ينتظر اعتمادي أو تنفيذي؟ وما الذي تم إنجازه؟

---

## §0 — الخلاصة التنفيذية

فحص النظام الحالي أظهر أن **أكثرية الآليات الداخلية موجودة فعلاً** (حالة «قيد الاعتماد»، الإرجاع للتعديل، خطة طويلة بأيام وورديات، ترحيل أوامر بتاريخ محدد، الإقفال اليومي يولّد أمر الفحص تلقائياً، سقف الاعتمادات، سجل تدقيق إلحاقي). الفجوة الحقيقية ليست في المنطق بل في **المرئية والاتجاه**: الموظف يدخل، يتصفح، يبحث، ويضغط «إضافة».

لذا تصميم B97 يقوم على ثلاث ركائز:

1. **المهمة المشتقة (لا جدول مهام جديد):** المهمة ليست سجلاً جديداً — هي «عرض حالي» على مستند قائم بحالة قابلة للفعل. لا جداول جديدة، لا تكرار بيانات، لا التزامن بين مهمة ومستندها. كل مستند بحالة قابلة للفعل = مهمة واحدة للدور المسؤول عنه.
2. **نمط واحد موحّد:** بطاقة مهمة في الواجهة ← نقرتان ← نافذة تفاصيل كاملة (قراءة فقط + تاريخ المستند) ← أزرار إجراء للمرحلة فقط. لا «إضافة ← بحث ← اختيار ← إدخال من جديد» في أي مكان من الدورة.
3. **قواعد انضباط خفيفة:** فصل صانع/مُعتمد (Four-Eyes)، سبب مكتوب إجباري لكل رفض أو إرجاع، وكل انتقال حالة يُسجَّل في سجل التدقيق (الآلية موجودة وتُوسَّع فقط).

ما يُضاف فعلياً في هذا التصميم: **طبقة «مهامي» على الواجهة الرئيسية** + **نوافذ تفاصيل موحّدة لكل مرحلة** + **قاعدة Four-Eyes** + **حقل سبب الحالة** (اختياري، صغير، يُرحَّل آلياً) — والباقي تفعيل وعرض لما هو قائم.

---

## §1 — خريطة الأدوار: من يفعل ماذا (على الأدوار والصلاحيات الموجودة)

لا أدوار جديدة مطلوبة. النظام يملك 7 أدوار (`SystemRoles`) وصلاحيات هرمية (موارد × عمليات) تُمنح للمستخدم الواحد من شاشة «الأدوار والصلاحيات». التوزيع المقترح للدورة:

| الشخص في الدورة | الدور (قائم) | ما يفعله | الصلاحيات (موارد/عمليات قائمة) |
|---|---|---|---|
| **المدير التنفيذي** | الإدارة (Management) — مستخدم «صانع» | ينشئ خطة الإنتاج ويعدّلها ويرسلها للاعتماد | `planning/Create` + `planning/Edit` + `planning/Post` (الترحيل) |
| **المدير العام** | الإدارة (Management) — مستخدم «معتمد» | يعتمد/يرفض/يُرجع الخطط؛ يرى كل الملخصات | `planning/Approve` (حساسة) + `planning/Cancel` (حساسة) |
| **مدير الإنتاج** | الإنتاج (Production) | يحرر أوامر التشغيل اليومية، يقفل اليوم، يحرر أوامر التسليم | `planning/Post` (ترحيل خطة→أوامر) + `production/*` + `execution/*` + `BypassInspection` (حساسة — التسليم المتجاوز) |
| **الجودة** | الجودة (Quality) | تفحص، تحكم (مقبول/مرفوض/محجوز)، تعتمد المحضر | `quality/Create` + `quality/Edit` + `quality/Approve` (حساسة) + `quality/EditAfterApproval` (حساسة) |
| **أمين المخزن** | المخازن (Warehouse) | يستلم التام إلى مخزن التام (كلي/جزئي) | `finishedgoods/*` + `inventory/View` |
| **التسليم/الفوترة** | المبيعات (Sales) / المالية (Finance) | تسليم العملاء + الفوترة | `delivery/Create` + `delivery/Approve` |
| **مدير النظام** | مدير النظام (Administrator) | الإعداد، المستخدمون، الصلاحيات، النسخ | كلها (تُستكمل تلقائياً) |

**قاعدة Four-Eyes (جديدة، إلزامية):** لا يُعتمد أي مستند من مستخدمه المنشئ. «المدير التنفيذي» و«المدير العام» مستخدمان مختلفان في النظام بصلاحيات مختلفة؛ لو حاول المنشئ الاعتماد نفسه → رفض واضح «المُعتمد لا يكون هو المنشئ» (سُيَّسَّل في التدقيق). هذه القاعدة تمنح معنى حقيقياً لمستوى الاعتماد الثاني.

> **قرارات اعتماد مطلوبة:**
> (أ) اعتماد توزيع الأدوار أعلاه (يمكن الإبقاء على مستخدم واحد «إدارة» يملك الصنعة والاعتماد معاً فقط في المصانع الصغيرة — Four-Eyes يصير «تنبيهي» لا «منعياً» بإعداد، ويُفضَّل المنع).
> (ب) سُميّات الأدوار في الواجهة: يُعرض للموظف اسمه الوظيفي (المدير التنفيذي/المدير العام...) لا رمز الدور.

---

## §2 — الدورة الكاملة: من المدير التنفيذي حتى الإغلاق

```
المدير التنفيذي
  │  ينشئ الخطة (يومية/أسبوعية/شهرية/فترة) — البيانات تدخل مرة واحدة هنا فقط
  ▼
[خطة: Draft] ─────────────────────────────┐
  │ يرسل للاعتماد                          │
  ▼                                       │
[خطة: UnderApproval] ──رفض/إرجاع (بسبب)──► تعود لصاحبها [Draft] + لافتة السبب
  │ اعتماد المدير العام (Four-Eyes)         │
  ▼                                       │
[خطة: Approved]  ◄────────────────────────┘  (تعديل ما قبل الاعتماد فقط)
  │
  │  لكل يوم في الفترة تولّد «مهمة يوم» تلقائياً (مشتقة — بلا سجل جديد):
  ▼
[مهمة اليوم: صفوف الخطة المستحقة]
  │  مدير الإنتاج: «إصدار أمر تشغيل لليوم» (محدود بتاريخ اليوم)
  ▼
[أمر إنتاج: Scheduled] ──بدء الوردية──► [InProgress]
  │                                      │ إيقاف (بسبب)
  │                                      ▼
  │                                   [Stopped] ──استئناف──► InProgress
  │  الإقفال اليومي (الفعلي المنتَج)
  ▼
[أمر إنتاج: Completed]
  │  النظام تلقائياً: أمر فحص «مُرسل للفحص» (التاريخ المتوقع = +2 تبريد)
  ▼
[فحص: Submitted] ─► [InProgress] ─► [Completed]
  │  اعتماد الجودة + الحكم
  ▼
[محضر: Approved — حكم: مطابق / غير مطابق / مرفوض / محجوز]
  │  مدير الإنتاج: «أمر تسليم إنتاج» (من المحضر المعتمد)
  ▼
[أمر تسليم: Issued]
  │  أمين المخزن: استلام إلى مخزن التام (كلي/جزئي لكل بند)
  ▼
[سند استلام: Full] ─► أمر التسليم مكتمل ─► الأمر يُغلق بإقفال الخطة/اليوم
  ▼
[تسليم عميل] ─► اعتماد ─► [فوترة: جزئي ← كامل]
  ▼
[الدورة: مكتملة] — التتبع الكامل متاح في أي لحظة (استلام ← فاتورة)
```

**ملاحظة صريحة (مرحلة التعبئة):** في دورة النظام الحالية **التعبئة جزء من الإنتاج** (نوع العبوة محدد على بند الخطة/الأمر والكرتون يُسجَّل عند الإقفال اليومي) وليست مرحلة مستقلة. التصميم لا يضيف مرحلة تعبئة؛ إن رأت الإدارة أنها مرحلة ذات مسؤولين مستقلين تُضاف في حزمة لاحقة بنفس نمط المهام.

**حالات الاستثناء المعتمدة في الدورة:**
- `مرفوض/إرجاع للتعديل` — في مرحلة الاعتماد (بسبب مكتوب).
- `متوقف` — أمر إنتاج أثناء التنفيذ (عطل/نقص مواد — بسبب، ويُستأنف).
- `مرفوض نهائي (عوادم)` و`محجوز (إعادة فحص)` — أحكام الجودة.
- `ملغى` — قبل أي أثر سفلي (أوامر/استلامات) فقط.
- `متأخر` — أي مهمة يوم/أمر تجاوز موعده دون تنفيذ (تنبيه أحمر، ليس حالة تخزين — مشتقة من التاريخ).

---

## §3 — حالات كل مرحلة (القيم الحالية — لا تغيير في الكيانات)

| المرحلة | الكيان | الحالات (القيم الموجودة) | من يحوّلها |
|---|---|---|---|
| الخطة | `ProductionPlan` | `Draft` ← `UnderApproval` ← `Approved` ← `Closed` / `Cancelled` (+إرجاع إلى Draft) | صانع/معتمد/مدير الإنتاج (الإقفال) |
| بند الخطة (يوم×وردية×عميل×صنف) | `ProductionPlanItem` | `ExecutionStatus`: `NotStarted` ← `InProgress` ← `Partial` ← `Completed` (+كميات: مخطط/منتج/مقبول/مسلَّم) | مشتق تلقائياً من التنفيذ والفحص والتسليم |
| أمر الإنتاج | `ProductionOrder` | `Scheduled` ← `InProgress` ← `Stopped` ← `Completed` ← `Closed` | مدير الإنتاج/النفّذ |
| أمر الفحص | `QualityCheck` | `Draft` ← `Submitted` (مُرسل للفحص) ← `InProgress` ← `Completed` ← `Approved` + `Decision`: Accepted/Rejected/Quarantine | الجودة |
| أمر تسليم الإنتاج | `ProductionDelivery` (B96) | `Draft` ← `Issued` + `ReceiptStatus`: None/Partial/Full | مدير الإنتاج ← أمين المخزن |
| سند استلام التام | `FinishedGoodsReceipt` (B96) | `Draft` ← `Issued` + استلام كلي/جزئي + `Unapprove` (عكس) | أمين المخزن |
| تسليم العميل | `CustomerDelivery` | `Draft` ← `Approved` | التسليم |
| الفوترة | (كمية على التسليم) | `InvoicedQtyKg < TotalQtyKg` ← كامل | المالية |
| الهالك/الثانوية | `Wastage` | `Draft` ← `Approved` | الجودة |

لا يُضاف أي Enum جديد إلزامياً؛ الحالة «متأخرة» والحالة «متعثرة» مشتقتان من البيانات (التاريخ + الحالة) فتبقى قاعدة البيانات كما هي.

---

## §4 — «مهامي»: ماذا يظهر تلقائياً لكل مستخدم (الواجهة الرئيسية)

الواجهة الرئيسية تحوَّل من «عدادات» إلى **مركز مهام** بثلاثة أعمدة موحّدة النمط + شريط تنبيهات حرجة (القائم يُوسَّع):

- 🔴 **بانتظار إجرائي** — مهام مضمّرة عليّ الآن (أقدمها أولاً، والمواعيد المتأخرة أعلى مع ⏰)
- 🟠 **قيد المسار** — ما بدأه مستخدمون آخرون في دوري (جلسات جارية، إيقافات، استلامات جزئية)
- 🟢 **أُنجز اليوم** — ما اكتمل خلال اليوم (سجل إنجازي)

**بطاقة المهمة (نموذج موحّد لكل الأدوار):**

```
🔔 خطة إنتاج بانتظار الاعتماد          [خطة: PL-2026-09-14]
   أنشأها: المدير التنفيذي (05/09/2026)
   الفترة: 07/09 – 21/09  |  العملاء: 3  |  الأصناف: 5
   الكمية: 12,400 كجم (413 كرتون)
   الحالة: بانتظار اعتمادك  |  المستحق: اليوم
   [نقرتان ← التفاصيل الكاملة]
```

### 4.1 — لوحة المدير التنفيذي
| العمود | المهمة | المصدر (قاعدة الاشتقاق) |
|---|---|---|
| 🔴 | **خطط أعيدت للتعديل** (مع لافتة: «أعادها المدير العام — السبب: ...») | خطة `Draft` لها ملاحظات إرجاع / حالة سبقتها UnderApproval |
| 🔴 | **خطط مسودة未完成** — راجعها وأرسلها (إن تجاوزت مسودتها N أيام) | خطة `Draft` لاكتملت بنودها |
| 🟠 | خطط مرشحة للاعتماد (بانتظار المدير العام) | خطة `UnderApproval` |
| 🟢 | خطط أرسلتها/اعتمدت اليوم | تاريخ الإرسال/الاعتماد = اليوم |
| تنبيه | ⏰ خطة معتمدة تأخر تشغيلها أكثر من يومين | أيام غير مكتملة دون أوامر |

أزراره: **تعديل** (مسودة/مرجعة)، **إرسال للاعتماد**، **عرض**.

### 4.2 — لوحة المدير العام
| العمود | المهمة | المصدر |
|---|---|---|
| 🔴 | **خطط إنتاج بانتظار اعتمادك** (منها: المنشئ، الفترة، العملاء، الأصناف، الكمية الإجمالية) | خطة `UnderApproval` |
| 🔴 | إيقافات إنتاج حالية (أمر متوقف + السبب + منذ متى) | أمر `Stopped` |
| 🟠 | خطط معتمدة قيد التنفيذ (تقدم يومي: منفذ/مخطط) | خطة `Approved` لاكتملت |
| 🟢 | خطط اعتمدتها اليوم / أغلقت اليوم | تاريخ = اليوم |
| تنبيه | ⏰ أوامر تجاوزت موعدها دون إقفال — 🚫 فحوصات مرفوضة/محجوزة — ⛔ خام تحت الحد — 🧾 تسليمات لم تُفوتر — 🛡️ تجاوزات فحص نشطة (أوامر تسليم متجاوزة — B96) | (الأربعة الأولى قائمة في اللوحة حالياً + الإضافة بقرار §15-5) |

أزراره (في نافذة التفاصيل): **اعتماد الخطة** / **إرجاع للتعديل (سبب إجباري)** / **إلغاء الاعتماد** (لخطة معتمدة بلا أوامر) / **عرض فقط** لغير صلاحياته.

### 4.3 — لوحة مدير الإنتاج
| العمود | المهمة | المصدر |
|---|---|---|
| 🔴 | **المطلوب تشغيله اليوم**: N أمر (تاريخ اليوم، العملاء، الأصناف، الكمية، الورديات) + ⏰ المتأخر من أيام سابقة | بنود خطة `Approved` بـ `ScheduledDate` اليوم (أو سابق غير مكتمل) بلا أوامر صادرة |
| 🔴 | أوامر جاهزة للبدء في موعدها (Scheduled وورديتها حاضرة) | أمر `Scheduled` |
| 🔴 | **فحوصات معتمدة بانتظار أمر تسليم** (B96) | محضر `Approved` له متبقي قابل للتسليم |
| 🟠 | جلسات تشغيل جارية (Progress %) — أوامر متوقفة (بسبب) | `InProgress` / `Stopped` |
| 🟢 | أيام أُقفلت اليوم — أوامر اكتملت | تاريخ الإقفال = اليوم |

أزراره: **إصدار أمر تشغيل لليوم** (يولّد أوامر لصفوف اليوم فقط — الترحيل الحالي يدعم النطاق التاريخي `fromDate/toDate`)، **بدء/استئناف/إيقاف (بسبب)**، **إقفال اليوم** (الفعلي المنتَج)، **أمر تسليم إنتاج** (من محضر معتمد — المسار الطبيعي، أو خطة/إقفال بتجاوز `BypassInspection` + سبب).

### 4.4 — لوحة الجودة
| العمود | المهمة | المصدر |
|---|---|---|
| 🔴 | **إنتاج جاهز للفحص**: أمر التشغيل، العميل، الصنف، الكمية، الوردية، تاريخ الإنتاج، المدة المتبقية للتبريد | أمر فحص `Submitted` (يُنشأ آلياً عند الإقفال اليومي) |
| 🔴 | محاضر محجوزة تنتظر إعادة الفحص بعد المعالجة | `Decision = Quarantine` |
| 🟠 | محاضر قيد الفحص (مسودة/مستمرة) | `InProgress` |
| 🟢 | فحوصات اعتمدت اليوم (مطابق/مرفوض/مقبول جزئي) | `Approved` تاريخ = اليوم |

أزرارها: **فتح الفحص** (كل البيانات مجمّعة آلياً من أمر الإنتاج — لا إعادة إدخال)، **تسجيل النتائج** (وحدها بيانات المرحلة)، **اعتماد المحضر + الحكم**، **تعديل بعد الاعتماد** (بسبب إجباري + سجل تصحيح — B95).

### 4.5 — لوحة أمين المخزن
| العمود | المهمة | المصدر |
|---|---|---|
| 🔴 | **أوامر تسليم بانتظار الاستلام** (العميل، الصنف، الدفعة، الكمية، مصدر الجودة: معتمد) | أمر تسليم `Issued` له متبقي |
| 🟠 | سندات استلام جزئية (المتبقي لكل بند) | `ReceiptStatus = Partial` |
| 🟢 | استلامات اكتملت اليوم | `Full` تاريخ = اليوم |

أزرارها: **تجهيز سند الاستلام** (البنود مجمّعة من أمر التسليم — العميل والكمية والدفعات جاهزة)، **استلام كلي/جزئي** (لكل بند)، **عكس الاستلام** (لصناديق قيد الاستلام).

### 4.6 — لوحة التسليم/الفوترة
| العمود | المهمة | المصدر |
|---|---|---|
| 🔴 | **سلّم جاهز للفوترة** (العميل، الكمية المسلَّمة، غير المفوتر) | تسليم معتمد `Invoiced < Total` |
| 🟠 | فوترة جزئية | — |
| 🟢 | فوترة اكتملت اليوم | — |

أزرارها: **تسليم عميل**، **اعتماد التسليم**، **فوترة** (بكمية — يمنع تكرار فوترة نفس الكمية).

> **ملاحظة B94 (تبقى نافذة):** لا يوجد زر يفتح شاشة أخرى داخل شاشة أخرى. بطاقة المهمة تفتح **نافذة تفاصيل منبثقة** (لا تنقّل)؛ والموظف ما زال يفتح شاشات إدارته من القائمة الجانبية كما هي تماماً — لا شيء يُحذف.

---

## §5 — نمط النقر المزدوج: نافذة التفاصيل الموحّدة

كل مهمة، بالنقرتين، تفتح نافذة موحّدة البنية من 4 طبقات:

1. **الرأس:** رقم المستند + الحالة (عربية ملونة) + المنشئ + تاريخه + المصدر (من أين جاءت) + (إن وُجد) لافتة سبب الإرجاع/الإيقاف.
2. **التفاصيل الكاملة (قراءة فقط — لا حقل إدخال إلا الخاص بالمرحلة):**
   - الخطة: العنوان، النوع/الفترة، العملاء، الأصناف، الكميات (كجم/كرتون)، الورديات، الطاقة المتوقعة لكل يوم، التوزيع اليومي (جدول الأيام: مخطط/منتج/مقبول/مسلَّم/حالة اليوم)، الملاحظات.
   - أمر التشغيل: بطاقة الأمر القائمة (OrderCard) + بنوده + الطاقة/الوردية/الخط + المواد المسحوبة.
   - الفحص: رأس المحضر (تام فقط، تاريخ الإنتاج من الأمر) + النتائج + معادلة التلخيص الحية (مفحوص = مقبول + مرفوض) + النسب من المنتَج.
   - التسليم/الاستلام: رأس + بنود (عميل/صنف/دفعة/كمية/متبقي) + الأبعاد (مستودع/دفعة/عميل).
3. **تاريخ المستند (الخط الزمني):** كل حدث: من، ماذا، متى، سبب — نمط سجل الأحداث القائم على أوامر الإنتاج يُعمَّم على كل المستندات في عرض.
4. **شريط الإجراء السفلي:** أزرار الدور × الحالة فقط (جدول §4) — بلا «إضافة»، بلا «حفظ جديد»، كل الإضافة في الدورة هي «المرحلة تضيف بياناتها» فقط.

**ماتستطيعه كل مرحلة إدخاله (لا غير):**

| المرحلة | ما يُدخله الموظف (الحد الأدنى) | ما يُجمع آلياً (لا يُعاد) |
|---|---|---|
| صنع الخطة | العملاء/الأصناف/الكميات/الأيام/الورديات | الطاقة المقترحة، حدود الخام |
| الاعتماد | السبب (في الإرجاع/الرفض) فقط | كل رأس الخطة وبنودها |
| أمر التشغيل | لا شيء — زر واحد لحدود اليوم | كل بندا من خطة اليوم |
| التنفيذ/الإقفال اليومي | الفعلي المنتَج + المواد الفعلية | المخطط، الفروقات، الكرتون |
| الفحص | النتائج (مقبول/مرفوض لكل صنف) | الأمر، الصنف، الكمية السقفية، التواريخ |
| أمر التسليم | المصدر + البنود (من الملء الآلي) | العميل/الدفعة/المتبقي من المحضر |
| الاستلام | الكمية المستلمة لكل بند | العميل/الصنف/الدفعة/السقف من التسليم |
| الفوترة | الكمية المفوترة | المسلَّم/المتبقي |

---

## §6 — ماذا يحدث عند الاعتماد / الرفض (آلية موحدة لكل المستندات)

### الاعتماد
1. فحص Four-Eyes (المُعتمد ≠ المنشئ) + فحص الصلاحية الحساسة (قائمان).
2. تغيير الحالة (مثلاً UnderApproval → Approved) + ختم المعتمد والتاريخ (حقول قائمة: `ApprovedBy/ApprovedDate/IsApproved`).
3. **سجل تدقيق إلزامي في نفس المعاملة** (قائم: `AuditSaveChangesInterceptor`): النوع `Approve` + القيمة القديمة → الجديدة + المستخدم + الجهاز.
4. **المهمة التالية تظهر تلقائياً** — لأنها مشتقة من الحالة: اعتماد الخطة ⇒ تختفي من «بانتظار اعتمادي» وتظهر فوراً في لوحة مدير الإنتاج «المطلوب تشغيله اليوم» يوم أول يوم مستحق. لا إخطار، لا نقل يدوي، لا شاشة ينقرها أحد.
5. **idempotency:** الضغط مرتين/مستخدمان في نفس اللحظة ⇒ الثاني يلقى «المستند معتمد مسبقاً» (حواجز قائمة) ولا تفسد البيانات (التزامن التفاؤلي قائم).

### الرفض / الإرجاع
1. **سبب مكتوب إجباري** (لا يقل عن 10 أحرف — يُرفض الحفظ بدونه)؛ يُعرض في نافذة الإرجاع كحقل نصوص إلزامي.
2. الحالة تعود إلى صاحبه (مثلاً UnderApproval → Draft) مع **حفظ السبب على المستند** (قريبياً في `Notes` بصيغة «سبب الإرجاع: ...» — و/أو حقل `StatusReason` إن عُدَّ الحقل الإضافي، §11).
3. **تظهر المهمة فوراً في العمود 🔴 لدى الصانع** بلوحة حمراء تحمل السبب في بطاقة نفسها.
4. الصانع يعدّل نفس المستند (لا مستند جديد) ثم يعيد الإرسال — يبقى الرقم والتاريخ الأصليان والتاريخ الكامل يُعرض في «تاريخ المستند».
5. كل خطوة (إرجاع، تعديل، إعادة إرسال) في سجل التدقيق.

### الإلغاء
- `Draft`/`UnderApproval`: يُلغى بحرية (حساسة + تدقيق).
- بعد أي أثر سفلي (أوامر صادرة/فحوصات/استلامات): **لا إلغاء** — الرسالة توجه إلى «الإقفال مع تسوية موثقة» (`CloseReason` — قائم). هذا يمنع «إخفاء» حركة تمت.

---

## §7 — الخطة الطويلة: كيف تظهر يومًا بيوم (01/09 → 14/09 مثال)

**القاعدة: الخطة الرئيسية واحدة؛ المهام اليومية مشتقة؛ الشاشة اليومية لا تطغى عليها الأيام المستقبلية.**

- كل بند خطة يحمل `ScheduledDate` + `SuggestedShiftId` + `SuggestedLineId` (قائم). لذلك «مهمة اليوم» حساب مباشر: **بنود الخطة المعتمدة التي تاريخها اليوم** (+ التي تأخرت من يوم سابق ولم تكتمل — تظهر بعلامة ⏰ في الأعلى).
- **ما لا يظهر في شاشة العمل اليومية:** الأيام المستقبلية — تبقى في «ملخص الخطة» (جدول الأيام القائم: مخطط/منتج/مقبول/مسلَّم/حالة) فقط.

**تسلسل اليوم (07/09 مثلاً):**

| اللحظة | ما يحدث | ما يظهر |
|---|---|---|
| صباح 07/09 | لا شيء يدوي — الاشتقاق تلقائي | بطاقة 🔴 لمدير الإنتاج: «المطلوب تشغيله اليوم 07/09 — 3 عملاء، 5 أصناف، XXX كجم، وردية أولى وثانية» |
| مدير الإنتاج يفتحها | تفاصيل اليوم فقط (لا كامل الخطة) | **زر: إصدار أمر التشغيل لليوم** → يولّد أوامر لصفوف اليوم (تجميع تاريخ×وردية×خط — منطق B93 الحالي محدود بنطاق `fromDate=toDate=اليوم`) |
| أثناء الوردية | بدء/تنفيذ/إيقاف | بطاقات 🟠: جلسات جارية (%) |
| نهاية الوردية | **إقفال اليوم**: الفعلي المنتَج + المواد الفعلية | أمر اليوم `Completed` + **أمر فحص يُنشأ تلقائياً** «مُرسل للفحص» (تاريخ متوقع +2) → يظهر فوراً 🔴 لدى الجودة |
| بعد +2 (تبريد) | الجودة تفحص وتُعتمد | المحضر `Approved` → يظهر 🔴 لدى مدير الإنتاج «فحص معتمد بانتظار أمر تسليم» |
| التسليم والاستلام | B96 (أمر تسليم ← سند استلام كلي/جزئي) | أمين المخزن يستلم؛ اكتمال الكمية ⇒ البند «مسلَّم» |
| بعد اكتمال اليوم | البند/اليوم يترك العمود 🔴 | اليوم 07/09 في «سجل التنفيذ»: ✅ مكتمل (مخطط/منتج/مقبول/مسلَّم/فروقات) — والغد 08/09 يظهر تلقائياً في الصبح |
| 14/09 (آخر يوم) | كل الأيام مكتملة أو بتسويات موثقة | الخطة تصبح جاهزة للإقفال النهائي |

**حالة اليوم المشتقة** (لجدول سجل التنفيذ): `مكتمل` (كل بندا: منتَج = مخطط أو مُسوّى) / `جزئي` / `غير مكتمل` / `لم يبدأ` — الحساب موجود فعلاً (`GetPlanDayStatuses`/`GetDailyPlan`).

**الإقفال النهائي للخطة الطويلة:** يُفتح من لوحة مدير الإنتاج (منها: الخطة + سجل أيامها) ⇒ نافذة الإقفال تعرض الفروقات النهائية (مخطط ↔ منتَج ↔ مقبول ↔ مسلَّم لكل عميل/صنف/يوم) + **سبب تسوية إجباري إن وُجد نقص** ⇒ `Closed` + معتمد + تاريخ. الخطة المغلقة: قراءة فقط، وتتبعها الكامل (تتبع الصنف القائم: استلام → فاتورة) يبقى متاحاً.

---

## §8 — الانتقال التلقائي بين المراحل (ماذا يحدث بلا ضغط أحد)

كلها آليات **موجودة في B95/B96**؛ B97 يكتفي بإظهار أثرها في لوحات المهام:

| الحدث (بصمته في النظام) | النتيجة الفورية | يظهر في لوحة |
|---|---|---|
| إقفال يوم الإنتاج (فعل: `CloseProductionDay`) | أمر فحص «مُرسل للفحص» + الكمية المنتجة + تاريخ الإنتاج + انتظار التبريد (+2) | 🔴 الجودة |
| اعتماد محضر الجودة بحكم «مطابق» | الكمية المطابقة تصبح «متاحة للتسليم» (سقف التسليم = مقبول المعتمد − المسلَّم سابقاً) | 🔴 مدير الإنتاج |
| تحرير أمر تسليم (Firm) | أمر التسليم `Issued` | 🔴 أمين المخزن |
| استلام كامل (كلي أو تراكمي) | أمر التسليم `Full` + أرصدة مخزن التام بأبعاد (مستودع/صنف/دفعة/**عميل**) | 🟢 أمين المخزن + 🔴 المالية (قابلة للفوترة) |
| تسليم عميل + اعتماد | الكمية «مسلَّمة» في تتبع الخطة والعميل | 🔴/🟢 المالية |
| فوترة كاملة | التسليم «مفوتر بالكامل» — تختفي من التنبيهات | 🟢 المالية |
| أي تأخر (تاريخ مستحق < اليوم) | المهمة تهبط من «قيد المسار» إلى 🔴 بعلامة ⏰ + تنبيه للمدير العام | اللوحات المعنية |

---

## §9 — سجل التدقيق (Audit): كل انتقال حالة، بلا استثناء

البنية القائمة (جدول `AuditLog` + وسيط يحفظ السجل **داخل نفس المعاملة** — «لا تدقيق بلا حركة ولا حركة بلا تدقيق») تُوسَّع بعرض فقط:

- **قانون صياغة الانتقال:** كل تغيّر حالة يكتب سجلًا: المستند + النوع + `القديمة ← الجديدة` + المستخدم + الجهاز + السبب (إن وُجد). أمثلة:
  - `خطة PL-2026-09-14: UnderApproval ← Approved — اعتمدها: المدير العام (ع.س) — 06/09/2026 09:12`
  - `خطة PL-2026-09-14: Approved ← Draft — إرجاع: «كمية يوم 10/09 تجاوز الطاقة المتوفرة» — المدير العام`
  - `أمر إنتاج PO-...: InProgress ← Stopped — سبب: عطل مفرمة الخط الثاني`
  - `فحص QC-...: Completed ← Approved — الحكم: مطابق (مقبول 900/مرفوض 80)`
- **العمليات الحساسة** (اعتماد/إلغاء/إعادة فتح/تجاوز الفحص/تعديل بعد الاعتماد) مُعلَّمة حساسة بالفعل في كتالوج الصلاحيات — تبقى، وكل محاولة فاشلة (بلا صلاحية) تسجَّل كـ `Denied` في التدقيق (إضافة عرضية صغيرة مقترحة).
- **تاريخ المستند** في نافذة التفاصيل (§5) = نفس السجلات مرتبة زمنياً مع الأسماء العربية — «من فعل ماذا ومتى ولماذا» دون البحث في سجل التدقيق الشامل.
- **سجل التدقيق الشامل** (الشاشة القائمة) يضيف فلاتر: بالمستند، بالمرحلة، بالنوع، بالمستخدم، بالفترة — ويصلح للتحقيق والرقابة.

---

## §10 — الاستثناءات: التأخير، الرفض، الإيقاف، العرقلات

| الحالة | السلوك | المساءلة |
|---|---|---|
| **تأخر تشغيل يوم** (تاريخه عبر ولم يصدر له أمر) | ⏰ أحمر في لوحة مدير الإنتاج + تنبيه للمدير العام + يظهر في «قيد المسار» كمهمة متعثرة | يُسأل في تقرير الإدارة: سبب التأخير يُسجَّل عند التشغيل المتأخر (حقل سبب اختياري يظهر فقط عند تأخر) |
| **رفض/إرجاع اعتماد** | §6 — سبب إجباري، عودة للصانع، نفس المستند | المعتمد يبرر، والصانع يرد بالتعديل |
| **إيقاف تنفيذ** (عطل/نقص) | أمر `Stopped` بسبب + بطاقة 🟠 في لوحتي مدير الإنتاج والمدير العام + «متوقف منذ» | الاستئناف يُسجَّل (زمن التوقف يُحسب لاحقاً لكفاءة الوردية) |
| **رفض جودة (مرفوض نهائي)** | الكمية → هالك/عوادم (سند هالك قائم) — **لا تسلَّم ولا تُستلم**؛ وأمر التسليم لا يستطيع سحبها (سقف التسليم = المقبول فقط) | القرار موثق + تدقيق |
| **حجز جودة (Quarantine)** | الكمية «محجوزة» — تظهر للجودة كمهمة 🔴 «إعادة فحص بعد المعالجة»؛ لا تدخل مخزن التام | المعالجة ثم إعادة الفحص |
| **فحص غير مطابق قابل للمعالجة** | (درجة «غير مطابق» القائمة) — الكمية تُعالج وتُعاد للفحص؛ أو تُنقَل لهالك — الخيار للجودة بحكم موثق | — |
| **تعارض متزامنتان** (مستخدمان يتصرفان على نفس المستند) | التزامن التفاؤلي القائم ⇒ الثاني: «المستند عدّل بعد قراءته — حدّث وعد» | — |
| **تجاوز الفحص** (تسليم بلا محضر) | ممكن فقط بصلاحية `BypassInspection` الحساسة + **سبب مكتوب إجباري** (B96) + تدقيق + يظهر في تنبيهات المدير العام «تجاوزات نشطة» | المسؤول عن التجاوز مرئي دائماً |

---

## §11 — قاعدة البيانات: التغيير المطلوب (وحدّه)

| البند | الحالة |
|---|---|
| جداول مهام | **لا شيء** — المهام مشتقة (§0). لا تكرار، لا التزامن، لا ترحيل. |
| حقل `StatusReason` (سبب الحالة: رفض/إرجاع/إيقاف/تسوية) على أساس المستندات | **اختياري ومقترح** — عمود نصي nullable واحد، يُرحَّل آلياً بموجّه الترحيل القائم (`SchemaMigrator` ينشئ الأعمدة الناقصة من النموذج عند الإقلاع — نمط مثبت في B95/B96). إن لم يُعتمد: البديل `Notes` + سجل التدقيق (أضعف قليلاً: السبب لا يظهر بارزاً على المستند). **قرار مطلوب.** |
| أي تغيير آخر | لا شيء. كل الحالات، الحقول، السجلات، الصلاحيات المستخدمة موجودة. |

---

## §12 — خريطة التنفيذ المقترحة (بعد الاعتماد — حزم مستقلة، كل واحدة: اختبارات + سجل إصلاح + ضغط + دفع)

| الحزمة | المحتوى | القيمة |
|---|---|---|
| **B97** | خدمة «مهامي» (اشتقاق المهام + البطاقات) + إعادة بناء مركز مهام الواجهة الرئيسية (3 أعمدة + تنبيهات) + **نافذة تفاصيل الخطة** + اعتماد/إرجاع/رفض المدير العام + Four-Eyes + `StatusReason` (إن عُدَّ) | الدورة من التنفيذي حتى أوامر التشغيل «موجَّهة» |
| **B98** | مهام الإنتاج: «المطلوب اليوم» + إصدار أمر اليوم (نطاق تاريخي) + بدء/إيقاف/استئناف + إقفال اليوم من مهمة + سجل تنفيذ الأيام (مكتمل/جزئي/متأخر) | قلب الدورة التشغيلية |
| **B99** | مهام الجودة + المخزن + التسليم/الفوترة (نوافذ التفاصيل + الأزرار + الاستلام الجزئي من المهمة) + نوافذ التاريخ الموحدة لكل المستندات | بقية الدورة «موجَّهة» |
| **B100** | التنبيهات المتعمقة (تأخر/إيقاف/تجاوزات نشطة) + فلاتر سجل التدقيق + تقرير الإقفال النهائي للخطة الطويلة + تظليل «المكلف/المعتمد» في كل البطاقات | الرقابة والإغلاق |

ملاحظات التنفيذ (تُفصَّل في كل حزمة): لا شاشة تُحذف؛ لا زر تنقّل بين الشاشات (B94)؛ النوافذ تفاصيل منبثقة؛ كل حزمة قابلة للإلغاء مستقلة (feature flag على مركز المهام إن أُريد).

---

## §13 — ما لا يُمَس (ضمانات)

- الشاشات الأربع والأربعون الحالية ووظائفها كاملة (لا حذف، لا دمج قسري — مركز المهام **يُضاف** فوق الواجهة الرئيسية).
- قاعدة B94: لا زر في شاشة ينقل إلى شاشة أخرى؛ كل موظف مختص بشاشته وصلاحياته.
- نظام الصلاحيات الهرمي (موارد × عمليات × حساسات) والعمل بحالة «مرفوض افتراضياً».
- كل المستندات والحدود والسقوف القائمة (تغطية مزدوجة، سقف الفحص، DUP_LINE، التزامن، تدقيق إلحاقي).
- قاعدة البيانات: لا تغيير إلزامي (§11).

---

## §14 — أسئلة الاعتماد (يُرجى الرد قبل التنفيذ)

1. **توزيع الأدوار (§1):** اعتماد خلية «صانع/معتمد» منفصلين (Four-Eyes منعياً) أم «تنبيهي» فقط؟
2. **حقل `StatusReason` (§11):** اعتماد العمود النصي الواحد (ترحيل آلي) أم الاكتفاء بـ `Notes` + التدقيق؟
3. **مرحلة التعبئة (§2):** تبقى ضمن الإنتاج (الحالي) أم تُفصل مرحلة مستقلة لاحقاً؟
4. **تسلسل الحزم (§12):** B97 → B98 → B99 → B100 أم ترتيب آخر حسب الأولوية التشغيلية؟
5. **أصناف «مهامي» لكل لوحة (§4):** أي بطاقة تُضاف/تُحذف من لوحة أي دور؟

## §15 — قرار المطوّر (2026-09-06 — يُعتمد إلا بمخالفة صريحة)

عند إحالة القرارات الخمسة إلى المطوّر («ما تراه أفضل؟»):

1. **Four-Eyes: منعياً.** مستخدمان منفصلان (صانع/معتمد) — «التنبيهي» يضيع في الأسبوع الأول، والمنع ما يمنح مستوى المدير العام سلطته الفعلية. رسالة الرفض توجه بوضوح إلى المعتمد المخوَّل.
2. **`StatusReason`: يُعتمد العمود النصي الواحد** (nullable، ترحيل آلي) — السبب يظهر لافتة على البطاقة والنافذة معاً؛ `Notes` حقل حر لا يليق بحمل تدقيق الرفض.
3. **التعبئة تبقى ضمن الإنتاج** (الحالي) — لا مرحلة لمنظمة بلا فريق تعبئة مستقل؛ وإن استقلت لاحقاً تُضاف بنفس نمط المهام.
4. **تسلسل B97 ← B98 ← B99 ← B100** كما §12 — كل حزمة قيمة مستقلة، وأي ترتيب آخر يؤخر الإحساس بالقيمة أو يكسر اتساق نمط النقر المزدوج.
5. **بطاقات §4 كما صُممت** (كلها مربوطة ببيانات حقيقية) + **إضافة واحدة:** تنبيه «تجاوزات فحص نشطة» (B96) في لوحة المدير العام — كل تجاوز حساس مرئي له دائماً، لا فقط عند وقوعه.

— **ملاحظة الحالة:** القرارات الخمسة حسمت بصلاحيات المطوّر. التنفيذ يبدأ بـ B97 فور اعتماد الإدارة لهذا المرفق.
