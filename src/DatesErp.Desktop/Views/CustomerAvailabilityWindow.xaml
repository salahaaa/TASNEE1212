<Window x:Class="DatesErp.Desktop.Views.CustomerAvailabilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="متاح العميل" Width="1000" Height="620"
        FlowDirection="RightToLeft" WindowStartupLocation="CenterOwner">
    <DockPanel Background="#ECE9D8">
        <!-- شريط العنوان -->
        <Border DockPanel.Dock="Top" Padding="12,6">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#0A246A" Offset="0"/>
                    <GradientStop Color="#3A6EA5" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel>
                <TextBlock x:Name="HeadTitle" DockPanel.Dock="Right" Foreground="White" FontWeight="Bold" FontSize="13"/>
                <TextBlock x:Name="HeadState" DockPanel.Dock="Left" Foreground="White" FontSize="12" FontWeight="Bold"/>
            </DockPanel>
        </Border>

        <!-- لافتة تعثر -->
        <Border x:Name="OverdueBanner" DockPanel.Dock="Top" Background="#FEF2F2" BorderBrush="#FCA5A5" BorderThickness="0,0,0,1" Padding="12,6">
            <TextBlock x:Name="OverdueText" Foreground="#991B1B" FontWeight="Bold" FontSize="12" TextWrapping="Wrap"/>
        </Border>

        <!-- الإجماليات -->
        <Border DockPanel.Dock="Top" Background="#F0F9FF" BorderBrush="#BAE6FD" BorderThickness="0,0,0,1" Padding="10,6">
            <WrapPanel>
                <Border Background="White" BorderBrush="#BAE6FD" BorderThickness="1" Padding="10,6" Margin="0,0,8,4">
                    <StackPanel>
                        <TextBlock Text="المخطط" FontSize="10.5" Foreground="#555"/>
                        <TextBlock x:Name="FPlanned" FontSize="13" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#BAE6FD" BorderThickness="1" Padding="10,6" Margin="0,0,8,4">
                    <StackPanel>
                        <TextBlock Text="المنتج" FontSize="10.5" Foreground="#555"/>
                        <TextBlock x:Name="FProduced" FontSize="13" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#BAE6FD" BorderThickness="1" Padding="10,6" Margin="0,0,8,4">
                    <StackPanel>
                        <TextBlock Text="في الفحص" FontSize="10.5" Foreground="#555"/>
                        <TextBlock x:Name="FInInspection" FontSize="13" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#BAE6FD" BorderThickness="1" Padding="10,6" Margin="0,0,8,4">
                    <StackPanel>
                        <TextBlock Text="المقبول" FontSize="10.5" Foreground="#555"/>
                        <TextBlock x:Name="FAccepted" FontSize="13" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="White" BorderBrush="#BAE6FD" BorderThickness="1" Padding="10,6" Margin="0,0,8,4">
                    <StackPanel>
                        <TextBlock Text="المسلَّم" FontSize="10.5" Foreground="#555"/>
                        <TextBlock x:Name="FDelivered" FontSize="13" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#ECFDF5" BorderBrush="#6EE7B7" BorderThickness="1" Padding="10,6">
                    <StackPanel>
                        <TextBlock Text="قابل للتسليم الآن ⚡" FontSize="10.5" Foreground="#065F46"/>
                        <TextBlock x:Name="FDeliverable" FontSize="14" FontWeight="Bold" Foreground="#059669"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </Border>

        <!-- شريط الإغلاق -->
        <Border DockPanel.Dock="Bottom" Background="#F4F2E8" BorderBrush="#7F9DB9" BorderThickness="0,1,0,0" Padding="10,8">
            <DockPanel>
                <Button Content="✖ إغلاق" Style="{StaticResource ErpButton}" DockPanel.Dock="Left" Click="Close_Click"/>
                <TextBlock DockPanel.Dock="Right" VerticalAlignment="Center" FontSize="11.5" Foreground="#555"
                           Text="عرض للقراءة — التسليم يُنفَّذ من شاشات الأدوار المختصة (بلا نقرات تنقل)"/>
            </DockPanel>
        </Border>

        <TabControl Margin="8" Background="White">
            <TabItem Header="📅 بالأيام">
                <DataGrid x:Name="DaysGrid" Margin="8" IsReadOnly="True" FontSize="11.5" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="اليوم" Binding="{Binding Date}" Width="110"/>
                        <DataGridTextColumn Header="المخطط (كجم)" Binding="{Binding PlannedKg}" Width="100"/>
                        <DataGridTextColumn Header="المنتج (كجم)" Binding="{Binding ProducedKg}" Width="100"/>
                        <DataGridTextColumn Header="المقبول (كجم)" Binding="{Binding AcceptedKg}" Width="100"/>
                        <DataGridTextColumn Header="المسلَّم (كجم)" Binding="{Binding DeliveredKg}" Width="100"/>
                        <DataGridTextColumn Header="الحالة" Binding="{Binding StatusAr}" Width="170"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="🏬 الدفعات المتاحة في مخزن التام">
                <DataGrid x:Name="StocksGrid" Margin="8" IsReadOnly="True" FontSize="11.5" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الصنف 🏷️" Binding="{Binding ProductName}" Width="*"/>
                        <DataGridTextColumn Header="العبوة" Binding="{Binding PackName}" Width="110"/>
                        <DataGridTextColumn Header="الدفعة" Binding="{Binding LotCode}" Width="110"/>
                        <DataGridTextColumn Header="المتاح (كجم)" Binding="{Binding QtyKg}" Width="110"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="🚚 سجل التسليم">
                <DataGrid x:Name="DeliveriesGrid" Margin="8" IsReadOnly="True" FontSize="11.5" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date}" Width="110"/>
                        <DataGridTextColumn Header="السند" Binding="{Binding DocumentNumber}" Width="130"/>
                        <DataGridTextColumn Header="الكمية (كجم)" Binding="{Binding QtyKg}" Width="110"/>
                        <DataGridTextColumn Header="الكراتين" Binding="{Binding Cartons}" Width="90"/>
                        <DataGridTextColumn Header="الحالة" Binding="{Binding StatusAr}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
