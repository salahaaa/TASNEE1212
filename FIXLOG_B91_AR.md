# سجل إصلاحات B91 — فحص الخطة متعددة العملاء بنفس عمل التوزيع (2026-09-05)

## §1 — الفكرة
زر «🔍 فحص الخطة» في شاشة التخطيط (بجانب «⚖ اقتراح توزيع عادل»): يأخذ الخطة المحفوظة
(عملاء/أصناف/فترة من–إلى) ويحاكي توزيع بنودها يوماً بيوم **بنفس قواعد محرك التوزيع العادل v2**،
ثم يحكم: ✅ قابلة للتنفيذ أو 🔴 فيها عجز — مع التفاصيل الكاملة.

## §2 — نفس عمل التوزيع (مطابقة صريحة)
- أيام العمل: تخطي الجمعة افتراضياً (`excludeFriday`) — الفترة بلا أيام عمل تُرفض بسببها.
- الورديات النشطة بالترتيب (وردية الخطة أولاً إن حُددت)؛ الساعات الفعالة وإلا 8.
- ترتيب العملاء: الأولوية (1 أولاً، 0 أخيراً) ثم الأقل إنجازاً — نفس الدوّار.
- رياضيات الطاقة نفسها: كراتين = أصغر (الطلب÷وزن العبوة، الساعات×المعدل، السقف−المستخدم)؛
  المعدل كرتون/ساعة من `CapacityPolicy.Resolve`؛ العبوة الافتراضية الأقرب لوزن الصنف.
- التحويل الرسمي (`ProductIdentityGuard`) لكل بند مربوط بدفعة — المخالف محجوب بصخب.
- الالتزامات الحية M8 (خطط + أوامر مستقلة) بلا ازدواج **وبلا احتساب ذاتي** (تُستثنى الخطة المفحوصة).
- عجز الدفعات لحظي ومشترك بين بنود الخطة؛ لا تجاوز للطاقة ولا صفوف صفرية أبداً.
- ملاحظة صدق: البنود تُوزَّع بحرية على الفترة (تواريخ البنود المحفوظة لا تقيّد الفحص — كالتوزيع).

## §3 — المخرجات (`PlanCheckResult` + نافذة `PlanCheckWindow`)
- شريط حكم أخضر/أحمر + سطر أرقام (أيام العمل/العملاء/البنود/المطلوب/المغطى/العجز).
- تبويب الأيام: مطلوب اليوم (حصة + مرحّل) / الموزع / الساعات / الحمل% / الحالة (مريح/ممتلئ/عجز/بلا حمل).
- تبويب العملاء: المطلوب/المغطى/العجز لكل عميل. تبويب الأصناف: + طاقة البند اليومية والأيام اللازمة.
- تبويب التحذيرات: كل تجاوز يقول سببه (بلا معدل/تحويل/خام/طاقة) + من أين جاءت المعدلات.

## §4 — الملفات (6 + اختبار)
1. `Core/.../IWorkflowServices.cs` — `CheckPlan` + نماذج `PlanCheck*`.
2. `Application/Services/PlanningService.cs` — محرك `CheckPlan` + `CheckDemand/CheckBucket`.
3. `Desktop/.../PlanningView.xaml` — زر الفحص. 4. `PlanningView.xaml.cs` — المعالج.
5. `Desktop/.../PlanningWindows.cs` — `PlanCheckWindow`.
6. `Services/BuildInfo.cs` — الختم B91. 7. `tests/.../PlanCheckB91Tests.cs` — جديد (6 اختبارات).

## §5 — التحقق على ويندوز
```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~PlanCheckB91Tests"
```
فحص يدوي: احفظ خطة متعددة العملاء لفترة أسبوع → «🔍 فحص الخطة» → الحكم والتوزيع اليومي والتغطيات.
