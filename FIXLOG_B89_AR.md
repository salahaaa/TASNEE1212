# سجل إصلاحات B89 — واجهات ملء الشاشة والفلترة الاحترافية (2026-09-05)

## §1 — ملء الشاشة: دخول أي وحدة يُخفي لوحة المعلومات/القائمة (الطلب §1)
- `MainWindow.xaml`: تسمية `SideBar` + `NavColumn` + زر `BackToDashBtn` («⟵ عودة إلى لوحة المعلومات») في شريط المسار — مخفي افتراضياً.
- `MainWindow.xaml.cs`: دالة `SetNavVisible(bool)` — تُخفي القائمة الجانبية وتصفّر عرض عمودها فيتمدد المحتوى على كامل العرض، وتُظهر زر العودة فقط والقائمة مخفية.
- `OpenScreen`: القائمة تظهر فقط في `dashboard` — أي وحدة أخرى (استلام/تخطيط/أوامر/جودة/تام/تسليم/مخازن/...) تملأ الشاشة تلقائياً. `OpenDeptDashboardPublic` يعيد القائمة دائماً.
- الاختصارات F2–F12 وشريط أدوات ErpChrome والقوائم السريعة (F12/Ctrl+K) لم تتغير — العمل كله بالفأرة ولوحة المفاتيح كما كان.

## §2 — الفلترة الاحترافية الإلزامية في القوائم العامة (الطلب §5) — 10 شاشات دفعة واحدة
- `GenericListView.xaml`: صف فلترة ثانٍ — «الحقل:» (`FieldBox`) + «القيمة:» (`ValueBox`) + عدّاد حي (`CountText`).
- `GenericListView.xaml.cs`:
  - `PopulateFilterBoxes`: بعد كل بحث تُبنى قائمة الحقول من أعمدة النتائج الفعلية («كل الحقول» + الأعمدة).
  - `PopulateValueBox`: قيم العمود المحدد — مميزة ومرتبة (بحد 200 قيمة) للاختيار المباشر بدل التخمين.
  - `ApplyFilter`: البحث النصي الفوري يشمل كل الحقول أو حقلاً واحداً + مرشح القيمة (مطابقة تامة) — يعملان معاً.
  - العدّاد الحي: «النتائج: N سجلاً» أو «النتائج: N من M سجلاً» أثناء الفلترة + عدّاد الإطار.
  - حارس `_populating` يمنع اشتعال الأحداث أثناء التعبئة البرمجية.
- الشاشات المستفيدة: العملاء/الموردون/الموظفون/المخازن/الدفعات/الهالك/الأرصدة/الحركات/الأجهزة/سجل التدقيق — كلها تفتح فارغة أصلاً (بحث إلزامي §2/§27).

## §3 — النقر المزدوج يدرّب في كل القوائم (الطلب §6)
- القوائم القابلة للتعديل: النقر المزدوج يفتح نافذة التعديل (كما كان).
- القوائم القرائية (الأرصدة/الحركات/الدفعات/الهالك/الأجهزة/التدقيق): كانت تتجاهل النقر المزدوج — الآن تفتح `RecordDetailsDialog`: بطاقة تفاصيل السجل كاملة عمودياً (كل حقل: قيمته) بخلفية مخططة — لا بيانات مخفية خلف عرض الشبكة.
- الشاشات التعاملية (الأوامر/الاستلام/التام/التسليم/التخطيط) فيها التدقيق بالنقر المزدوج أصلاً؛ شاشات التحرير الخالية (الجودة/الصلاحيات/المستخدمون) النقر فيها للتحرير/التحديد المباشر فلا يناسبها التدقيق المنبثق.

## §4 — تلميع الثيم (إضافات فقط — لا تغيير مفاتيح ولا كسر شاشات)
- `DateErpTheme.xaml`:
  - الحقل النشط (TextBox) يُعرف فوراً بإطار كحلي `#1D4ED8`؛ الحقل القرائي بخلفية رمادية مميزة.
  - صف الجدول تحت المؤشر يُبرز (`#E8EEF7`) قبل لون التحديد فلا يغلبه.
  - قالب خطأ تحقق عام: إطار أحمر + ⚠ مع رسالة الخطأ — يظهر تلقائياً متى استخدمت أي شاشة قواعد التحقق.

## §5 — الملفات المتغيرة (6 + ملف اختبار جديد)
1. `src/DatesErp.Desktop/Views/MainWindow.xaml` — تسمية + زر العودة.
2. `src/DatesErp.Desktop/Views/MainWindow.xaml.cs` — `SetNavVisible` + الربط.
3. `src/DatesErp.Desktop/Views/Screens/GenericListView.xaml` — صف الفلترة.
4. `src/DatesErp.Desktop/Views/Screens/GenericListView.xaml.cs` — الفلاتر + العدّاد + التدقيق.
5. `src/DatesErp.Desktop/Themes/DateErpTheme.xaml` — التلميع.
6. `src/DatesErp.Desktop/Services/BuildInfo.cs` — الختم B89.
7. `tests/DatesErp.Tests/UiB89Tests.cs` — جديد (5 اختبارات بنيوية).

## §6 — التحقق على ويندوز
```powershell
dotnet build DateERP.sln -c Release
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~UiB89Tests"
dotnet test tests/DatesErp.Tests/DatesErp.Tests.csproj -c Release --filter "FullyQualifiedName~UiWiringTests"
```
فحص يدوي (5 دقائق):
1. افتح أي وحدة (الأوامر/الاستلام/...) — القائمة الجانبية تختفي والنموذج يملأ العرض + زر «عودة إلى لوحة المعلومات» ظاهر.
2. ارجع للوحة — القائمة تعود والزر يختفي.
3. افتح الأرصدة/الحركات: ابحث، صفِّ بالحقل ثم بالقيمة، لاحظ العدّاد الحي، وانقر نقراً مزدوجاً — بطاقة التفاصيل تظهر بكل الحقول.
4. مرر المؤشر على أي جدول — الصف يُبرز؛ انقر أي حقل نصي — إطاره كحلي.
