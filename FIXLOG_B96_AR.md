# سجل التطوير B96 — فصل أمر تسليم الإنتاج عن سند الاستلام المخزني (2026-09-05)

## §0 — القرار (طلب الإدارة)
فصل مسؤوليتين اختلطتا في مستند واحد: (أ) **أمر تسليم إنتاج** يحرره مدير الإنتاج
داخل إدارة الإنتاج (بلا أي أثر على الأرصدة)، (ب) **سند استلام** ينفذه أمين المخزن
داخل إدارة المخازن (وهو وحده ما يورّد التام). كلاهما يدعم عميلاً أو عدة عملاء ×
صنفاً أو أكثر — البند = أمر + صنف + دفعة + عميل + كمية — بلا قفز بين الشاشات (قاعدة B94:
كل موظف في شاشته وصلاحياته).

## §1 — أمر تسليم الإنتاج (جديد)
- الخدمة `ProductionDeliveryService` + الكيانان `ProductionDelivery` / `ProductionDeliveryItem`
  (الترقيم `PDL` — الترحيل التلقائي ينشئ الجدولين عند الإقلاع، وقواعد البيانات القديمة لا تُمس).
- المصادر الثلاثة (`DeliverySources`): محضر فحص **معتمد** (المسار الطبيعي) / خطة إنتاج /
  إقفال خطة (الأخيران تجاوز للفحص — §2).
- الملء الآلي `GetSourceContext`: سطور المصدر مع المتاح/المُسلَّم/المتبقي — من المحضر:
  المقبول المعتمد لكل (صنف + دفعة)؛ ومن الخطة/الإقفال: **المنتَج المزامَن فقط**
  (لا المخطط النظري) لكل بند خطة.
- سقفان معاً: سقف المصدر (لا تسليم فوق المتبقي القابل للتسليم) + **السقف الفيزيائي الموحد**
  (كل أوامر التسليم + المستلَم المباشر ≤ إنتاج الأمر — يمنع ازدواج محضر+خطة لنفس الإنتاج).
- مطابقة صارمة لسطر المصدر (صنف + دفعة + عميل) + مطابقة الدفعة لدفعة بند الأمر
  (لا استبدال هوية) + التام فقط (002) عبر `RequireItemType`.
- دورة الأمر: مسودة ← مُحرَّر (اعتماد الإنتاج) ← مستلم (جزئي/كامل عبر المخازن)؛
  الإلغاء مسموح للمسودة دائماً وللمُحرَّر ما لم يبدأ استلامه.

## §2 — التجاوز بصلاحية وسبب مكتوب (لا تجاوز صامت)
- التسليم من الخطة/الإقفال يتطلب صلاحية `BypassInspection` على وحدة الإنتاج
  (موجودة في `OperationCatalog` كعملية **حساسة** — تُمنح صراحةً لمدير الإنتاج،
  ومرفوضة افتراضياً لغيره) + **سبب مكتوب إجباري** يُحفظ في `BypassReason` على الأمر.
- كل تسليم بتجاوز يُسجَّل في التدقيق (المصدر + السبب) — قابل للمراجعة اللاحقة.
- الفحص اليدوي بلا أمر لا يُسلَّم منه إطلاقاً (التسليم من إنتاج أمر تشغيل فقط).

## §3 — سند الاستلام المخزني (توسعة `FinishedGoodsService` — لا كسر للقديم)
- وضعان في الشاشة الجديدة: (أ) من أمر تسليم مُحرَّر — العميل والكمية من بند التسليم،
  (ب) مباشر من الأمر — المسار القديم بحذافيره (بوابة الفحص/التبريد + سقوفه كما كانت).
- المربوط: سقف بند التسليم (المتبقي) + السقف الفيزيائي الموحد (مباشر + مربوط ≤ المنتَج) —
  يُفحصان عند الحفظ **ويُعاد فحصهما عند الاستلام** (مسودتان متوازيتان لا تتجاوزان معاً).
- الاستلام الجزئي يتراكم على البند والسند والأمر (`None/Partial/Full`)؛ اكتمال الأمر
  يُكمله تلقائياً؛ إلغاء السند قيد عكسي (لا حذف) يعكس بند التسليم ويعيد فتح الأمر المكتمل.
- رفض مبكر واضح لبندين بنفس (الصنف + الدفعة) في سند واحد (`DUP_LINE`) — كان يفشل لاحقاً
  برسالة «حركة مكررة» مضللة؛ والمخرج النظامي لتقسيم دفعة على عميلين: سند مستقل لكل بند.
- التام يُورَّد بأبعاد (مستودع + صنف + دفعة + **عميل البند**) — فعميل كل بند محفوظ في رصيده.

## §4 — الشاشتان (تسجيل + أكواد)
- `proddelivery` — أوامر تسليم الإنتاج — `MRPMPS1021` — وحدة الإنتاج:
  اختيار المصدر ← تنزيل السطور ← تضمين/تعديل الكميات ← حفظ ← تحرير/إلغاء.
- `fgreceive` — أوامر استلام الإنتاج — `MRPINV1006` — وحدة المخازن والاستلام:
  وضع من-تسليم/مباشر ← تنزيل ← حفظ ← إصدار ← استلام (كلي/جزئي لكل بند) ← إلغاء.
- لا زر ينقل بينهما (B94)؛ الشاشة القديمة باقية للتوافق ولا يُكسر أي مستدعٍ
  (`SaveReceipt` توسعت بمعامل اختياري `deliveryId = null`).

## §5 — الاختبارات (8 في `B96DeliveryTests` — الكل يعمل بلا اعتماد على بيانات خارجية)
1. التسليم من محضر معتمد: ملء آلي 490 + رفض أي كيلو فوق المتبقي.
2. رفض المحضر غير المعتمد («غير معتمد») والفحص اليدوي («اليدوي»).
3. سحب `BypassInspection` ← `PermissionDeniedException`؛ بلا سبب ← رفض «سبب»؛
   بهما ← قبول والسبب محفوظ في الأمر.
4. من الإقفال: خطة غير مقفلة ← «مقفلة»؛ بعد `CloseOrder` + `ClosePlanFinal` ← قبول.
5. عميلان × دفعتان: التام (490 لعميل 1 + 480 لعميل 2) بأبعاد صحيحة والأمر `Full`.
6. جزئي 200 ← `Partial`؛ سند 291 ← «المتبقي»؛ استكمال 290 ← `Full` + مكتمل.
7. إلغاء أمر مستلم ← «مستلم»؛ إلغاء السند يعكس (رصيد صفر) ويعيد الفتح؛ الإلغاء يُقبل.
8. المسار المباشر القديم بلا تغيير («الجودة سمحت» + 500 كجم لعميل الأمر).

## §6 — ملاحظات للمشغّل
- امنح «الإنتاج/تجاوز الفحص» لمدير الإنتاج فقط من شاشة الصلاحيات (حساسة).
- قواعد البيانات القائمة تترقى ذاتياً عند أول إقلاع (جداول `ProductionDelivery*` +
  عمودا `CustomerId`/`DeliveryItemId` في بنود التام) — لا خطوة يدوية.
- الأوامر القديمة المسلَّمة مباشرة لا تتأثر إطلاقاً (لا `DeliveryId` = المسار الأصلي).
